* County Adjustments
* Alaska 
qui replace county = "ANCHORAGE" if (city=="ANCHORAGE"|county=="ANCHRAGE"|county=="MUNICAPAiLITY OF ANCH."|county=="MUNICAPALITY OF ANCHORAGE") & state=="AK"
qui replace county = "BETHEL" if city=="BETHEL" & state=="AK"
qui replace county = "CORDOVA-MCCARTHY" if county=="CORDOVA" & state=="AK"
qui replace county = "FAIRBANKS NORTH STAR" if (city=="FAIRBANKS"|county=="NORTH STAR") & state=="AK"
qui replace county = "JUNEAU" if city=="JUNEAU" & state=="AK"
qui replace county = "KENAI PENINSULA" if (city=="HOMER"|city=="SOLDOTNA"|city=="SEWARD") & state=="AK"
qui replace county = "KETCHIKAN GATEWAY" if (county=="GATEWAY"|county=="KETCHIKAN"|city=="KETCHIKAN") & state=="AK"
qui replace county = "NORTHWEST ARTIC BOROUGH" if county=="KOBUK" & state=="AK"
qui replace county = "KODIAK ISLAND BOROUGH" if (city=="KODIAK"|county=="KODIAK") & state=="AK"
qui replace county = "MATANUSKA-SUSITNA" if (city=="PALMER"|county=="MANTANUSKA-SUSITKA BOROUGH"|county=="MATANUSKA-SUSITKA BOROUGH") & state=="AK"
qui replace county = "NOME" if city=="NOME" & state=="AK"
qui replace county = "NORTH SLOPE BOROUH" if county=="NORTH SLOPE" & state=="AK"
qui replace county = "SITKA BOROUGH" if (city=="SITKA"|county=="CITY & OF SITKA BOROUGH"|county=="SITKA") & state=="AK"
qui replace county = "VALDEZ-CORDOVA" if (city=="CORDOVA"|city=="VALDEZ"|county=="VALDEZ CORDOVA") & state=="AK"
qui replace county = "WRANGELL CITY AND BOROUGH" if county=="WRANGELL" & state=="AK"
qui replace county = "WRANGELL-PETERSBURG" if (city=="PETERSBURG"|city=="WRANGELL") & state=="AK"
qui replace county = "WRANGELL CITY AND BOROUGH" if county=="WRANGELL-PETERSBURG" & state=="AK"

* Alabama 
qui replace county = "BALDWIN" if (city=="BAY MINETTE"|city=="DAPHNE"|city=="FAIRHOPE") & state=="AL" & county=="."
qui replace county = "BALDWIN" if city=="FAIRHOPE" & state=="AL" & county=="RURAL"
qui replace county = "BARBOUR" if city=="EUFAULA" & state=="AL" & county=="."
qui replace county = "BIBB" if city=="CENTREVILLE" & state=="AL" & county=="."
qui replace county = "BULLOCK" if city=="UNION SPRINGS" & state=="AL" & county=="."
qui replace county = "BUTLER" if (city=="GEORGIANA"|city=="GEROGIANA"|city=="GREENVILLE") & state=="AL" & county=="."
qui replace county = "BUTLER" if (county=="UTLER"|county=="BUTLERN") & state=="AL"
qui replace county = "CALHOUN" if (city=="ANNISTON"|city=="JACKSONVILLE") & state=="AL" & county=="."
qui replace county = "CHAMBERS" if city=="VALLEY" & state=="AL" & county=="."
qui replace county = "CLARKE" if (city=="GROVE HILL"|city=="THOMASVILLE") & state=="AL" & county=="."
qui replace county = "COFFEE" if city=="ELBA" & state=="AL" & county=="."
qui replace county = "COLBERT" if (city=="MUSCLE SHOALS"|city=="SHEFFIELD") & state=="AL" & county=="."
qui replace county = "CONECUH" if city=="EVERGREEN" & state=="AL" & county=="."
qui replace county = "COVINGTON" if (city=="ANDALUSIA"|city=="FLORALA"|city=="OPP") & state=="AL" & county=="."
qui replace county = "CRENSHAW" if city=="LUVERNE" & state=="AL" & county=="."
qui replace county = "CULLMAN" if city=="CULLMAN" & state=="AL" & county=="."
qui replace county = "CULLMAN" if county=="CULMAN" & state=="AL"
qui replace county = "DALE" if city=="OZARK" & state=="AL" & county=="."
qui replace county = "DALLAS" if city=="SELMA" & state=="AL" & county=="."
qui replace county = "DE KALB" if city=="FORT PAYNE" & state=="AL" & county=="."
qui replace county = "DE KALB" if county=="DEKALB" & state=="AL"
qui replace county = "ELMORE" if (city=="PRATTVILLE"|city=="TALLASSEE"|city=="WETUMPKA") & state=="AL" & county=="."
qui replace county = "ESCAMBIA" if (city=="ATMORE"|city=="BREWTON") & state=="AL" & county=="."
qui replace county = "ETOWAH" if (city=="BOAZ"|city=="GADSDEN") & state=="AL" & county=="."
qui replace county = "FRANKLIN" if (city=="RED BAY"|city=="RUSSELLVILLE") & state=="AL" & county=="."
qui replace county = "GENEVA" if (city=="GENEVA"|city=="EUTAW") & state=="AL" & county=="."
qui replace county = "HALE" if city=="GREENSBORO" & state=="AL" & county=="."
qui replace county = "HENRY" if city=="DOTHAN" & state=="AL" & county=="."
qui replace county = "JACKSON" if city=="BRIDGEPORT" & state=="AL" & county=="."
qui replace county = "JEFFERSON" if (city=="BESSEMER"|city=="BIRMINGHAM"|city=="BIRMINHAM"|city=="FAIRFIELD") & state=="AL" & county=="."
qui replace county = "JEFFERSON" if (county=="JEFFERSONY"|county=="JEFFERSEON"|county=="BIRMINGHAM") & state=="AL"
qui replace county = "LAUDERDALE" if city=="FLORENCE" & state=="AL" & county=="."
qui replace county = "LAWRENCE" if city=="MOULTON" & state=="AL" & county=="."
qui replace county = "LIMESTONE" if (city=="ATHENS"|city=="DECATUR"|city=="HUNTSVILLE") & state=="AL" & county=="."
qui replace county = "MARENGO" if city=="DEMOPOLIS" & state=="AL" & county=="."
qui replace county = "MARION" if (city=="HALEYVILLE"|city=="HAMILTON"|city=="WINFIELD") & state=="AL" & county=="."
qui replace county = "MARSHALL" if city=="GUNTERSVILLE" & state=="AL" & county=="."
qui replace county = "MOBILE" if (city=="MOBILE"|city=="MT VERNON") & state=="AL" & county=="."
qui replace county = "MONTGOMERY" if city=="MONTGOMERY" & state=="AL" & county=="."
qui replace county = "PICKENS" if city=="CARROLLTON" & state=="AL" & county=="."
qui replace county = "RANDOLPH" if (city=="ROANOKE"|city=="WEDOWEE") & state=="AL" & county=="."
qui replace county = "RANDOLPH" if county=="ROANOKE" & state=="AL"
qui replace county = "RUSSELL" if city=="PHENIX CITY" & state=="AL" & county=="."
qui replace county = "SHELBY" if city=="ALABASTER" & state=="AL" & county=="."
qui replace county = "SUMTER" if city=="YORK" & state=="AL" & county=="."
qui replace county = "TALLADEGA" if (city=="SYLACAUGA"|city=="TALLADEGA") & state=="AL" & county=="."
qui replace county = "TALLADEGA" if county=="TALLEDEGA" & state=="AL"
qui replace county = "TALLAPOOSA" if (city=="ALEXANDER CITY"|city=="DADEVILLE") & state=="AL" & county=="."
qui replace county = "TUSCALOOSA" if (city=="NORTHPORT"|city=="TUSCALOOSA") & state=="AL" & county=="."
qui replace county = "WASHINGTON" if city=="CHATOM" & state=="AL" & county=="."
qui replace county = "WILCOX" if city=="CAMDEN" & state=="AL" & county=="."

* Arkansas 
qui replace county = "ARKANSAS" if county=="AR" & state=="AR"
qui replace county = "CLEBURNE" if county=="CLEBURNENG" & state=="AR"
qui replace county = "COLUMBIA" if city=="MAGNOLIA" & state=="AR" & county=="."
qui replace county = "CRAIGHEAD" if county=="GRAIGHEAD" & state=="AR"
qui replace county = "CRAWFORD" if county=="SUMMIT" & state=="AR"
qui replace county = "GARLAND" if city=="HOT SPRINGS" & state=="AR" & county=="."
qui replace county = "HEMPSTEAD" if city=="HOPE" & state=="AR" & county=="."
qui replace county = "HEMPSTEAD" if county=="HEMSTEAD" & state=="AR"
qui replace county = "JEFFERSON" if city=="PINE BLUFF" & state=="AR" & county=="."
qui replace county = "PHILLIPS" if county=="PHILLILPS" & state=="AR"
qui replace county = "PULASKI" if (city=="LITTLE ROCK"|city=="MAUAMELLE"|city=="MAUMELLE") & state=="AR" & county=="."
qui replace county = "PULASKI" if (county=="PULSKI"|county=="PULASHI"|county=="PALASKI") & state=="AR"
qui replace county = "SALINE" if city=="BENTON" & state=="AR" & county=="."
qui replace county = "SEBASTIAN" if (county=="SEBASTIANN"|county=="SEBASTION") & state=="AR"
qui replace county = "SEBASTIAN" if city=="FORT SMITH" & state=="AR" & county=="."
qui replace county = "SEVIER" if city=="DEQUEEN" & state=="AR" & county=="."
qui replace county = "SEVIER" if county=="SERIER" & state=="AR"
qui replace county = "ST. FRANCIS" if (county=="ST. FANCIS"|county=="ST.") & state=="AR"
qui replace county = "ST. FRANCIS" if city=="FORREST CITY" & state=="AR" & county=="."
qui replace county = "VAN BUREN" if county=="VAN" & state=="AR"
qui replace county = "WASHINGTON" if city=="FAYETTEVILLE" & state=="AR" & county=="."
qui replace county = "WHITE" if city=="SEARCY" & state=="AR" & county=="."

* Arizona 
qui replace county = "APACHE" if (city=="CHINLE"|city=="FORT DEFIANCE"|city=="GANADO") & state=="AZ" & county=="."
qui replace county = "APACHE" if (county=="BERNAALILLO"|county=="BERNALILLO"|county=="FT. DEFIANCE"|county=="FT DEFIANCE") & state=="AZ"
qui replace county = "COCHISE" if county=="DOUGLAS" & state=="AZ"
qui replace county = "COCONINO" if city=="TUBA CITY" & state=="AZ" & county=="."
qui replace county = "GILA" if (city=="CLAYPOOL"|city=="SAN CARLOS") & state=="AZ" & county=="."
qui replace county = "GILA" if city=="CLAYPOOL" & state=="AZ" & county=="COUNTY"
qui replace county = "GILA" if county=="GILANLEY" & state=="AZ"
qui replace county = "GILA" if zip == "85501" & state=="AZ" & county=="."
qui replace county = "LAPAZ" if (county=="LA PAZ"|county=="PARKER") & state=="AZ"
qui replace county = "MARICOPA" if (city=="CHANDLER"|city=="GLENDALE"|city=="MESA"|city=="PHOENIX"|city=="SCOTTSDALE"|city=="SUN CITY") & state=="AZ" & county=="."
qui replace county = "MARICOPA" if city=="PHEINOX" & state=="AZ" & county=="SAN CAROLS"
qui replace county = "MARICOPA" if (county=="PHOENIX"|county=="MAROCOPA") & state=="AZ"
qui replace county = "NAVAJO" if city=="WHITERIVER" & state=="AZ" & county=="."
qui replace county = "NAVAJO" if city=="POLACCA" & state=="AZ" & county=="UNKNOWN"
qui replace county = "NAVAJO" if (county=="NAJAVO"|county=="NAVYO") & state=="AZ"
qui replace county = "PIMA" if (city=="SELLS"|city=="TUCSON"|city=="TUSCON") & state=="AZ" & county=="."
qui replace county = "PIMA" if county=="HARDIN" & state=="AZ"
qui replace county = "YAVAPAI" if county=="YAVAPAII" & state=="AZ"
qui replace county = "YUMA" if city=="YUMA" & state=="AZ" & county=="."

* California 
qui replace county = subinstr(county,"  CALIFORNIA","",.) if state=="CA"
qui replace county = subinstr(county,"  CA","",.) if state=="CA"
qui replace county = substr(county,1,strlen(county)-11) if substr(county,-11,11)==" CALIFORNIA" & state=="CA"
qui replace county = substr(county,1,strlen(county)-3) if substr(county,-3,3)==" CA" & state=="CA"
qui replace county = "ALAMEDA" if (city=="FREMONT"|city=="SAN LEANDRO") & state=="CA" & county=="."
qui replace county = "ALAMEDA" if (county=="ALMEDA"|county=="OAKLAND") & state=="CA"
qui replace county = "BUTTE" if city=="PARADISE" & state=="CA" & county=="."
qui replace county = "CONTRA COSTA" if (city=="MARTINEZ"|city=="SAN RAMON"|city=="WALNUT CREEK") & state=="CA" & county=="."
qui replace county = "CONTRA COSTA" if county=="CONTR COSTA" & state=="CA"
qui replace county = "DEL NORTE" if county=="DELNORTE" & state=="CA"
qui replace county = "EL DORADO" if city=="SOUTH LAKE TAHOE" & state=="CA" & county=="."
qui replace county = "EL DORADO" if county=="EL" & state=="CA"
qui replace county = "FRESNO" if (city=="FRESNO"|city=="SELMA") & state=="CA" & county=="."
qui replace county = "HUMBOLDT" if (city=="EUREKA"|city=="FORTUNA") & state=="CA" & county=="."
qui replace county = "KERN" if (city=="BAKERSFIELD"|city=="TAFT") & state=="CA" & county=="."
qui replace county = "LASSEN" if city=="SUSANVILLE" & state=="CA" & county=="."
qui replace county = "LOS ANGELES" if (city=="ARTESIA"|city=="BELLFLOWER"|city=="CERRITOS"|city=="COVINA"|city=="DOWNEY"|city=="DUARTE"|city=="ENCINO"|city=="GRANADA HILLS"|city=="HOLLYWOOD"|city=="LANCASTER"|city=="LONG BEACH"|city=="LOS ANGELES"|city=="MONROVIA"|city=="NORTHRIDGE"|city=="NORWALK"|city=="PANORAMA CITY"|city=="PASADENA"|city=="POMONA"|city=="ROSEMEAD"|city=="SAN DIMAS"|city=="SAN PEDRO"|city=="SANTA MONICA"|city=="SHERMAN OAKS"|city=="TARZANA"|city=="TORRANCE"|city=="VAN NUYS") & state=="CA" & county=="."
qui replace county = "LOS ANGELES" if city=="LOS ANGELES" & state=="CA" & county=="07"
qui replace county = "LOS ANGELES" if (county==" LOS ANGELES"|county=="LA"|county=="LOS ANGEES"|county=="LOS ANGELEES"|county=="LOS"|substr(county,1,11)=="LOS ANGELES") & state=="CA"
qui replace county = "LOS ANGELES" if zip == "90036-4404" & county=="." & state=="CA"
qui replace county = "MARIN" if (city=="KENTFIELD"|city=="NOVATO") & state=="CA" & county=="."
qui replace county = "MARIN" if county=="WAYNE" & state=="CA"
qui replace county = "MONO" if city=="MAMMOTH LAKES" & state=="CA" & county=="."
qui replace county = "NAPA" if (city=="IMOLA"|city=="NAPA") & state=="CA" & county=="."
qui replace county = "ORANGE" if (city=="ANAHEIM"|city=="BREA"|city=="COSTA MESA"|city=="HUNTINGTON BEACH"|city=="LA HABRA"|city=="MISSION VEIJO"|city=="MISSION VIEJO"|city=="ORANGE"|city=="TUSTIN"|city=="WESTMINISTER"|city=="WESTMINSTER") & state=="CA" & county=="."
qui replace county = "ORANGE" if county=="ORGANE" & state=="CA"
qui replace county = "PLACER" if (city=="AUBURN"|city=="ROSEVEILLE"|city=="ROSEVILLE") & state=="CA" & county=="."
qui replace county = "RIVERSIDE" if (city=="CATHEDERAL CITY"|city=="CATHEDRAL CITY"|city=="CORONA"|city=="MURRIETA") & state=="CA" & county=="."
qui replace county = "SACRAMENTO" if (city=="FOLSOM"|city=="SACRAMENTO") & state=="CA" & county=="."
qui replace county = "SAN BERNARDINO" if (city=="APPLE VALLEY"|city=="CHINO"|city=="LOMA LINDA"|city=="NEEDLES"|city=="ONTARIO"|city=="REDLANDS"|city=="SAN BERNARDINO") & state=="CA" & county=="."
qui replace county = "SAN BERNARDINO" if city=="SAN BERNARDINO" & state=="CA" & county=="S"
qui replace county = "SAN BERNARDINO" if (county=="SAN BERNADINO"|county=="SAN BERNANDINO"|county=="SAN BERNARDINIO") & state=="CA"
qui replace county = "SAN DIEGO" if (city=="CHULA VISTA"|city=="CORONADO"|city=="ENCINITAS"|city=="LA MESA"|city=="SAN DIEGO") & state=="CA" & county=="."
qui replace county = "SAN FRANCISCO" if county=="SAN FRANCISO" & state=="CA"
qui replace county = "SAN JOAQUIN" if (county=="SAN JAUQUIN"|county=="SAN JAOQUIN") & state=="CA"
qui replace county = "SAN LUIS OBISPO" if county=="SAN LUIS ABISPO" & state=="CA"
qui replace county = "SAN MATEO" if city=="BELMONT" & state=="CA" & county=="."
qui replace county = "SANTA BARBARA" if (county=="SANTA BARABARA"|county=="SB") & state=="CA"
qui replace county = "SANTA BARBARA" if city=="SANTA BARBARA" & state=="CA" & county=="."
qui replace county = "SANTA CLARA" if (city=="SAN JOSE"|city=="SANTA CLARA") & state=="CA" & county=="."
qui replace county = "SISKIYOU" if city=="YREKA" & state=="CA" & county=="."
qui replace county = "SISKIYOU" if county=="SUSKIYOU" & state=="CA"
qui replace county = "SOLANO" if county=="SOLANA" & state=="CA"
qui replace county = "SONOMA" if (city=="ELDRIDGE"|city=="SEBASTAPAL"|city=="SEBASTAPOL") & state=="CA" & county=="."
qui replace county = "SONOMA" if county=="SANTA ROSA" & state=="CA"
qui replace county = "SUTTER" if city=="YUBA CITY" & state=="CA" & county=="."
qui replace county = "TULARE" if city=="PORTERVILLE" & state=="CA" & county=="."
qui replace county = "TUOLUMNE" if city=="SONORA" & state=="CA" & county=="."
qui replace county = "TUOLUMNE" if county=="TOULUMNE" & state=="CA"

* Colorado 
qui replace county = "ADAMS" if (city=="AURORA"|city=="THORNTON") & state=="CO" & county=="."
qui replace county = "ADAMS" if county=="AURORA" & state=="CO"
qui replace county = "ARAPAHOE" if city=="ENGLEWOOD" & state=="CO" & county=="."
qui replace county = "ARAPAHOE" if county=="ARAPHAHOE" & state=="CO"
qui replace county = "BOULDER" if (city=="BOULDER"|city=="LOUISVILLE") & state=="CO" & county=="."
qui replace county = "DELTA" if city=="DELTA" & county=="D" & state=="CO"
qui replace county = "DELTA" if city=="DELTA" & state=="CO" & county=="."
qui replace county = "DENVER" if city=="DENVER" & state=="CO" & county=="."
qui replace county = "DENVER" if zip == "80206-" & county=="." & state=="CO"
qui replace county = "EL PASO" if (county=="ELPASO"|county=="EL") & state=="CO"
qui replace county = "EL PASO" if city=="COLORADO SPRINGS" & state=="CO" & county=="."
qui replace county = "FREMONT" if city=="CANON CITY" & state=="CO" & county=="."
qui replace county = "FREMONT" if county=="FREEMONT" & state=="CO"
qui replace county = "GARFIELD" if city=="GLENWOOD SPRINGS" & state=="CO" & county=="."
qui replace county = "JEFFERSON" if city=="WHEAT RIDGE" & state=="CO" & county=="."
qui replace county = "LA PLATA" if (county=="DURANGO"|county=="LAPLATA") & state=="CO"
qui replace county = "LAKE" if city=="LEADVILLE" & state=="CO" & county=="."
qui replace county = "LARIMER" if (county=="FORT COLLINS"|county=="LARMIER") & state=="CO"
qui replace county = "LAS ANIMAS" if city=="TRINIDAD" & state=="CO" & county=="."
qui replace county = "MOFFAT" if city=="CRAIG" & state=="CO" & county=="."
qui replace county = "MOFFAT" if county=="MOFFETT" & state=="CO"
qui replace county = "MONTROSE" if city=="MONTROSE" & state=="CO" & county=="."
qui replace county = "MORGAN" if city=="FORT MORGAN" & state=="CO" & county=="."
qui replace county = "PUEBLO" if city=="PUEBLO" & state=="CO" & county=="."
qui replace county = "RIO BLANCO" if county=="RANGELY" & state=="CO"

* Connecticut 
qui replace county = "FAIRFIELD" if (city=="BRIDGEPORT"|city=="GREENWICH"|city=="NORWALK"|city=="WESTPORT") & state=="CT" & county=="."
qui replace county = "HARTFORD" if (city=="HARTFORD"|city=="MANCHESTER"|city=="WEST HARTFORD") & state=="CT" & county=="."
qui replace county = "HARTFORD" if county=="HARTFORD CT" & state=="CT"
qui replace county = "LITCHFIELD" if (city=="NEW MILFORD"|city=="SHARON"|city=="TORRINGTON") & state=="CT" & county=="."
qui replace county = "MIDDLESEX" if (city=="MIDDLETOWN"|city=="PORTLAND") & state=="CT" & county=="."
qui replace county = "NEW HAVEN" if (city=="DERBY"|city=="MERIDEN"|city=="NEW HAVEN"|city=="WATERBURY") & state=="CT" & county=="."
qui replace county = "NEW HAVEN" if (county=="N"|county=="EAST. HAVEN"|county=="WATERBURY") & state=="CT"
qui replace county = "NEW LONDON" if city=="NORWICH HOSPITAL" & state=="CT" & county=="."
qui replace county = "TOLLAND" if city=="VERNON" & state=="CT" & county=="."
qui replace county = "TOLLAND" if county=="T" & state=="CT"
qui replace county = "WINDHAM" if city=="WILLIMANTIC" & state=="CT" & county=="."
qui replace county = "WINDHAM" if county=="W" & state=="CT"

* District of Columbia
qui replace county = "THE DISTRICT" if state=="DC"

* Delaware 
qui replace county = "KENT" if county=="DOVEROMERY" & state=="DE"
qui replace county = "NEW CASTLE" if (county=="NEWCASTLE"|county=="NEW") & state=="DE"
qui replace county = "NEW CASTLE" if (city=="NEW CASTLE"|city=="NEWARK"|city=="WILMINGTON") & state=="DE" & county=="."
qui replace county = "SUSSEX" if city=="LEWES" & state=="DE" & county=="."

* Florida 
qui replace county = "ALACHUA" if city=="GAINESVILLE" & state=="FL" & county=="."
qui replace county = "BAKER" if city=="MACCLENNY" & state=="FL" & county=="."
qui replace county = "BAKER" if county=="NA" & city=="MACCLENNY" & state=="FL"
qui replace county = "BAY" if city=="PANAMA CITY" & state=="FL" & county=="."
qui replace county = "BREVARD" if (city=="MELBOURNE"|city=="TITUSVILLE") & state=="FL" & county=="."
qui replace county = "BREVARD" if county=="BREVARDY" & state=="FL"
qui replace county = "BROWARD" if (city=="FORT LAUDERDALE"|city=="FT LAUDERDALE"|city=="FT. LAUDERDALE"|city=="HOLLYWOOD"|city=="MARGATE"|city=="PEMBROKE PINES"|city=="PLANTATION"|city=="SUNRISE") & state=="FL" & county=="."
qui replace county = "BROWARD" if (county=="DADA"|county=="FT. LAUDERDALE") & state=="FL"
qui replace county = "COLUMBIA" if city=="LAKE CITY" & state=="FL" & county=="."
qui replace county = "DE SOTO" if city=="ARCADIA" & state=="FL" & county=="."
qui replace county = "DE SOTO" if county=="DESOTO" & state=="FL"
qui replace county = "DE SOTO" if county=="NA" & city=="ARCADIA" & state=="FL"
qui replace county = "DUVAL" if city=="JACKSONVILLE" & state=="FL" & county=="."
qui replace county = "DUVAL" if county=="JACKSONVILLE" & state=="FL"
qui replace county = "ESCAMBIA" if city=="PENSACOLA" & state=="FL" & county=="."
qui replace county = "FRANKLIN" if county=="FRANKILN" & state=="FL"
qui replace county = "GADSDEN" if (city=="CHATAHOOCHEE"|city=="CHATTACHOOCHEE") & state=="FL" & county=="."
qui replace county = "GADSDEN" if county=="NA" & city=="CHATTACHOOCHEE" & state=="FL"
qui replace county = "GLADES" if county=="EVERGLADES" & state=="FL"
qui replace county = "HAMILTON" if city=="JASPER" & state=="FL" & county=="."
qui replace county = "HARDEE" if county=="ARCADIA" & state=="FL"
qui replace county = "HILLSBOROUGH" if (city=="LUTZ"|city=="SUN CITY CENTER"|city=="TAMPA") & state=="FL" & county=="."
qui replace county = "HILLSBOROUGH" if county=="HILLSBOROUGJ" & state=="FL"
qui replace county = "HOLMES" if county=="HOLMS" & state=="FL"
qui replace county = "INDIAN RIVER" if city=="VERO BEACH" & state=="FL" & county=="."
qui replace county = "JACKSON" if city=="MARIANNA" & state=="FL" & county=="."
qui replace county = "JACKSON" if county=="JACKSONCOUNTY" & state=="FL"
qui replace county = "LEE" if (city=="CAPE CORAL"|city=="FORT MEYERS"|city=="FT. MYERS"|city=="LEHIGH ACRES") & state=="FL" & county=="."
qui replace county = "LEON" if city=="TALLAHASSEE" & state=="FL" & county=="."
qui replace county = "MANATEE" if city=="BRADENTON" & state=="FL" & county=="."
qui replace county = "MARION" if (city=="OCALA"|city=="STUART") & state=="FL" & county=="."
qui replace county = "MIAMI-DADE" if (city=="MIAMI"|city=="SOUTH MIAMI") & state=="FL" & county=="."
qui replace county = "MIAMI-DADE" if (county=="DADE"|county=="MAIMI-DADE"|county=="MIAMI - DADE"|county=="MIAMI DADE") & state=="FL"
qui replace county = "MONROE" if city=="KEY WEST" & state=="FL" & county=="."
qui replace county = "OKALOOSA" if (city=="CRESTVIEW"|city=="FORT WALTON BCH"|city=="FORT WALTON BEACH"|city=="FORT WALTON") & state=="FL" & county=="."
qui replace county = "ORANGE" if city=="ORLANDO" & state=="FL" & county=="."
qui replace county = "OSCEOLA" if (city=="KISSIMMEE"|city=="KISSISSIMME") & state=="FL" & county=="."
qui replace county = "OSCEOLA" if county=="OCALA" & state=="FL"
qui replace county = "PALM BEACH" if (city=="JUPITER"|city=="LANTANA"|city=="PALM BEACH GARDENS"|city=="WEST PALM BEACH") & state=="FL" & county=="."
qui replace county = "PALM BEACH" if (county=="PALM"|county=="WEST. PALM BEACH") & state=="FL"
qui replace county = "PASCO" if (city=="HUDSON"|city=="NEW PORT RICHEY"|city=="WESLEY CHAPEL") & state=="FL" & county=="."
qui replace county = "PINELLAS" if (city=="LARGO"|city=="SEMINOLE"|city=="ST. PETERSBURG") & state=="FL" & county=="."
qui replace county = "POLK" if (city=="LAKELAND"|city=="WINTER HAVEN") & state=="FL" & county=="."
qui replace county = "POLK" if city=="WINTER HAVEN" & state=="FL"
qui replace county = "POLK" if county=="PLOK" & state=="FL"
qui replace county = "PUTNAM" if city=="PALATKA" & state=="FL"
qui replace county = "PUTNAM" if city=="PALATKA" & state=="FL" & county=="."
qui replace county = "SANTA ROSA" if city=="MILTON" & state=="FL"
qui replace county = "SANTA ROSA" if city=="MILTON" & state=="FL" & county=="."
qui replace county = "SARASOTA" if (city=="SARASOTA"|city=="VENICE") & state=="FL" & county=="."
qui replace county = "SARASOTA" if city=="SARASOTA" & state=="FL"
qui replace county = "SARASOTA" if city=="VENICE" & state=="FL"
qui replace county = "SEMINOLE" if city=="APOPKA" & state=="FL"
qui replace county = "SEMINOLE" if city=="APOPKA" & state=="FL" & county=="."
qui replace county = "ST. LUCIE" if city=="PORT ST. LUCIE" & state=="FL"
qui replace county = "ST. LUCIE" if city=="PORT ST. LUCIE" & state=="FL" & county=="."
qui replace county = "VOLUSIA" if (city=="ORANGE CITY"|city=="ORMOND BEACH") & state=="FL" & county=="."
qui replace county = "VOLUSIA" if city=="ORANGE CITY" & state=="FL"
qui replace county = "VOLUSIA" if city=="ORMOND BEACH" & state=="FL"
qui replace county = "VOLUSIA" if county=="VOLUASIA" & state=="FL"

* Georgia 
qui replace county = "BACON" if city=="ALMA" & state=="GA" & county=="."
qui replace county = "BALDWIN" if city=="MILLEDGEVILLE" & state=="GA" & county=="."
qui replace county = "BALDWIN" if county=="BALWIN" & state=="GA"
qui replace county = "BARROW" if city=="WINDER" & state=="GA" & county=="."
qui replace county = "BEN HILL" if (county=="BENHILL"|county=="BEN") & state=="GA"
qui replace county = "BEN HILL" if city=="FITZGERALD" & state=="GA" & county=="."
qui replace county = "BIBB" if city=="MACON" & state=="GA" & county=="."
qui replace county = "BLECKLEY" if city=="COCHRAN" & state=="GA" & county=="."
qui replace county = "BLECKLEY" if county=="BLECKLY" & state=="GA"
qui replace county = "CAMDEN" if city=="ST. MARYS" & state=="GA" & county=="."
qui replace county = "CANDLER" if city=="METTER" & state=="GA" & county=="."
qui replace county = "CARROLL" if city=="BREMEN" & state=="GA" & county=="."
qui replace county = "CARROLL" if county=="C" & (city=="CARROLLTON"|city=="VILLA RICA") & state=="GA"
qui replace county = "CHATHAM" if city=="SAVANNAH" & state=="GA" & county=="."
qui replace county = "CLARKE" if city=="ATHENS" & state=="GA" & county=="."
qui replace county = "CLARKE" if county=="CLARK" & state=="GA"
qui replace county = "CLAYTON" if county=="C" & city=="RIVERDALE" & state=="GA"
qui replace county = "CLINCH" if city=="HOMERVILLE" & state=="GA" & county=="."
qui replace county = "COBB" if (city=="MARIETTA"|city=="SMYRNA") & state=="GA" & county=="."
qui replace county = "COLQUITT" if county=="COLQUITTCOUNTY" & state=="GA"
qui replace county = "COOK" if city=="ADEL" & state=="GA" & county=="."
qui replace county = "COWETA" if city=="NEWNAN" & state=="GA" & county=="."
qui replace county = "DE KALB" if (city=="DECATUR"|city=="TUCKER") & state=="GA" & county=="."
qui replace county = "DE KALB" if county=="DEKALB" & state=="GA"
qui replace county = "DODGE" if city=="EASTMAN" & state=="GA" & county=="."
qui replace county = "DOOLY" if city=="VIENNA" & state=="GA" & county=="."
qui replace county = "DOUGHERTY" if city=="ALBANY" & state=="GA" & county=="."
qui replace county = "DOUGLAS" if (city=="DOUGLASVILLE"|city=="LITHIA SPRINGS") & state=="GA" & county=="."
qui replace county = "DOUGLAS" if county=="DOUGALS" & state=="GA"
qui replace county = "EARLY" if city=="ARLINGTON" & state=="GA" & county=="."
qui replace county = "EFFINGHAM" if city=="SPRINGFIELD" & state=="GA" & county=="."
qui replace county = "FAYETTE" if city=="FAYETTEVILLE" & state=="GA" & county=="."
qui replace county = "FAYETTE" if county=="F" & city=="FAYETTEVILLE" & state=="GA"
qui replace county = "FLOYD" if city=="ROME" & state=="GA" & county=="."
qui replace county = "FORSYTH" if city=="CUMMING" & state=="GA" & county=="."
qui replace county = "FULTON" if (city=="ATLANTA"|city=="COLLEGE PARK"|city=="EAST POINT") & state=="GA" & county=="."
qui replace county = "FULTON" if county=="F" & city=="ATLANTA" & state=="GA"
qui replace county = "GILMER" if city=="ELLIJAY" & state=="GA" & county=="."
qui replace county = "GLYNN" if (city=="BRUNSWICK"|city=="SIMONS ISLAND"|city=="ST SIMONS ISLAND"|city=="ST. SIMONS ISLAND") & state=="GA" & county=="."
qui replace county = "GLYNN" if (county=="G"|county=="GLENN") & state=="GA"
qui replace county = "GWINNETT" if (city=="LAWRENCEVILLE"|city=="SNELLVILLE") & state=="GA" & county=="."
qui replace county = "HABERSHAM" if city=="DEMOREST" & state=="GA" & county=="."
qui replace county = "HALL" if (city=="GAINESVILLE"|city=="GAINSVILLE") & state=="GA" & county=="."
qui replace county = "HANCOCK" if city=="SPARTA" & state=="GA" & county=="."
qui replace county = "HARALSON" if county=="H" & state=="GA"
qui replace county = "HENRY" if city=="STOCKBRIDGE" & state=="GA" & county=="."
qui replace county = "HOUSTON" if (city=="PERRY"|city=="WARNER ROBBINS"|city=="WARNER ROBINS") & state=="GA" & county=="."
qui replace county = "HOUSTON" if county=="PERRY" & state=="GA"
qui replace county = "JASPER" if city=="MONTICELLO" & state=="GA" & county=="."
qui replace county = "JEFF DAVIS" if (city=="HAZELHURST"|city=="HAZLEHURST") & state=="GA"
qui replace county = "JEFFERSON" if city=="LOUISVILLE" & state=="GA" & county=="."
qui replace county = "JEFFERSON" if county=="JEFFERSSON" & state=="GA"
qui replace county = "JENKINS" if city=="MILLEN" & state=="GA" & county=="."
qui replace county = "LANIER" if city=="LAKELAND" & state=="GA" & county=="."
qui replace county = "LAURENS" if city=="DUBLIN" & state=="GA" & county=="."
qui replace county = "LIBERTY" if city=="HINESVILLE" & state=="GA" & county=="."
qui replace county = "LOWNDES" if (county=="1156 LOWNDES"|county=="LOWDER") & state=="GA"
qui replace county = "LOWNDES" if city=="VALDOSTA" & state=="GA" & county=="."
qui replace county = "LUMPKIN" if city=="DAHLONEGA" & state=="GA" & county=="."
qui replace county = "MACON" if city=="MONTEZUMA" & state=="GA" & county=="."
qui replace county = "MC DUFFIE" if city=="THOMSON" & state=="GA" & county=="."
qui replace county = "MC DUFFIE" if county=="MCDUFFIE" & state=="GA"
qui replace county = "MERIWETHER" if city=="WARM SPRINGS" & state=="GA" & county=="."
qui replace county = "MILLER" if city=="COLQUITT" & state=="GA" & county=="."
qui replace county = "MITCHELL" if city=="CAMILLA" & state=="GA" & county=="."
qui replace county = "MORGAN" if city=="MADISON" & state=="GA" & county=="."
qui replace county = "MUSCOGEE" if city=="COLUMBUS" & state=="GA"
qui replace county = "NEWTON" if city=="COVINGTON" & state=="GA" & county=="."
qui replace county = "PEACH" if city=="FORT VALLEY" & state=="GA" & county=="."
qui replace county = "PUTNAM" if city=="EATONTON" & state=="GA" & county=="."
qui replace county = "RANDOLPH" if city=="CUTHBERT" & state=="GA" & county=="."
qui replace county = "RICHMOND" if city=="AUGUSTA" & state=="GA" & county=="."
qui replace county = "SEMINOLE" if city=="DONALSONVILLE" & state=="GA" & county=="."
qui replace county = "STEWART" if city=="RICHLAND" & state=="GA" & county=="."
qui replace county = "SUMTER" if city=="AMERICUS" & state=="GA" & county=="."
qui replace county = "TATTNALL" if city=="REIDSVILLE" & state=="GA" & county=="."
qui replace county = "TELFAIR" if city=="MCRAE" & state=="GA" & county=="."
qui replace county = "THOMAS" if city=="THOMASVILLE" & state=="GA" & county=="."
qui replace county = "TIFT" if city=="TIFTON" & state=="GA" & county=="."
qui replace county = "TOOMBS" if city=="VIDALIA" & state=="GA" & county=="."
qui replace county = "TOOMBS" if county=="TOOBMS" & state=="GA"
qui replace county = "TOWNS" if city=="HIAWASSEE" & state=="GA" & county=="."
qui replace county = "TROUP" if city=="LAGRANGE" & state=="GA" & county=="."
qui replace county = "UPSON" if city=="THOMASTON" & state=="GA" & county=="."
qui replace county = "WALKER" if (county=="WALKER DADE AND CATOOSA"|county=="WALKER DADE AND CATOSSA") & state=="GA"
qui replace county = "WALTON" if city=="MONROE" & state=="GA" & county=="."
qui replace county = "WARE" if city=="WAYCROSS" & state=="GA" & county=="."
qui replace county = "WASHINGTON" if city=="SANDERSVILLE" & state=="GA" & county=="."
qui replace county = "WORTH" if city=="SYLVESTER" & state=="GA" & county=="."

* Hawaii 
qui replace county = "HAWAII" if (city=="HILO"|city=="HONOKAA"|city=="KAILUA"|city=="KAPAAU"|city=="KEALAKEKUA"|city=="PAHALA") & state=="HI" & county=="."
qui replace county = "HONOLULU" if (city=="EWA BEACH"|city=="HONOLULU"|city=="WAHIAWA") & state=="HI" & county=="."
qui replace county = "KAUAI" if (city=="KAPAA"|city=="LIHUE"|city=="WAIMEA  KAUAI") & state=="HI" & county=="."
qui replace county = "MAUI" if (city=="KAUNAKAKAI"|city=="WAILUKA"|city=="WAILUKU") & state=="HI" & county=="."
qui replace county = "MAUI" if county=="MAUAI" & state=="HI"
qui replace county = "HONOLULU" if (county=="HONOULU"|county=="OAHU") & state=="HI"

* Iowa 
qui replace county = "ADAIR" if city=="GREENFIELD" & state=="IA" & county=="."
qui replace county = "ALLAMAKEE" if city=="WAUKON" & state=="IA" & county=="."
qui replace county = "APPANOOSE" if city=="CENTERVILLE" & state=="IA" & county=="."
qui replace county = "AUDUBON" if city=="AUDUBON" & state=="IA" & county=="."
qui replace county = "BENTON" if city=="VINTON" & state=="IA" & county=="."
qui replace county = "BLACK HAWK" if (city=="CEDAR FALLS"|city=="WATERLOO") & state=="IA" & county=="."
qui replace county = "BLACK HAWK" if county=="BLACKHAWK" & state=="IA"
qui replace county = "BOONE" if city=="BOONE" & state=="IA" & county=="."
qui replace county = "BREMER" if city=="WAVERLY" & state=="IA" & county=="."
qui replace county = "BUCHANAN" if city=="INDEPENDENCE" & state=="IA" & county=="."
qui replace county = "BUENA VISTA" if city=="STORM LAKE" & state=="IA" & county=="."
qui replace county = "CALHOUN" if city=="LAKE CITY" & state=="IA" & county=="."
qui replace county = "CARROLL" if city=="CARROLL" & state=="IA" & county=="."
qui replace county = "CASS" if city=="ATLANTIC" & state=="IA" & county=="."
qui replace county = "CHEROKEE" if city=="CHEROKEE" & state=="IA" & county=="."
qui replace county = "CHICKASAW" if city=="NEW HAMPTON" & state=="IA" & county=="."
qui replace county = "CLAYTON" if (city=="ELKADER"|city=="GUTTENBERG") & state=="IA" & county=="."
qui replace county = "CLINTON" if (city=="DEWITT"|city=="MAQUOKETA") & state=="IA" & county=="."
qui replace county = "CRAWFORD" if city=="DENISON" & state=="IA" & county=="."
qui replace county = "DALLAS" if city=="PERRY" & state=="IA" & county=="."
qui replace county = "DELAWARE" if city=="MANCHESTER" & state=="IA" & county=="."
qui replace county = "DES MOINES" if city=="WEST BURLINGTON" & state=="IA" & county=="."
qui replace county = "DICKINSON" if city=="SPIRIT LAKE" & state=="IA" & county=="."
qui replace county = "DUBUQUE" if city=="DUBUQUE" & state=="IA" & county=="."
qui replace county = "EMMET" if city=="ESTHERVILLE" & state=="IA" & county=="."
qui replace county = "FAYETTE" if (city=="OELWEIN"|city=="SUMNER"|city=="WEST UNION") & state=="IA" & county=="."
qui replace county = "FLOYD" if city=="CHARLES CITY" & state=="IA" & county=="."
qui replace county = "FRANKLIN" if city=="HAMPTON" & state=="IA" & county=="."
qui replace county = "FREMONT" if city=="HAMBURG" & state=="IA" & county=="."
qui replace county = "GRUNDY" if city=="GRUNDY CENTER" & state=="IA" & county=="."
qui replace county = "GUTHRIE" if city=="GUTHRIE CENTER" & state=="IA" & county=="."
qui replace county = "HAMILTON" if city=="WEBSTER CITY" & state=="IA" & county=="."
qui replace county = "HAMILTON" if county=="HAMILTON CO" & state=="IA"
qui replace county = "HANCOCK" if city=="BRITT" & state=="IA" & county=="."
qui replace county = "HARDIN" if (city=="ELDORA"|city=="IOWA FALLS") & state=="IA" & county=="."
qui replace county = "HENRY" if city=="MT. PLEASANT" & state=="IA" & county=="."
qui replace county = "HOWARD" if city=="CRESCO" & state=="IA" & county=="."
qui replace county = "IDA" if city=="IDA GROVE" & state=="IA" & county=="."
qui replace county = "IOWA" if city=="MARENGO" & state=="IA" & county=="."
qui replace county = "JASPER" if city=="NEWTON" & state=="IA" & county=="."
qui replace county = "JEFFERSON" if city=="FAIRFIELD" & state=="IA" & county=="."
qui replace county = "JOHNSON" if city=="IOWA CITY" & state=="IA" & county=="."
qui replace county = "KEOKUK" if city=="SIGOURNEY" & state=="IA" & county=="."
qui replace county = "LEE" if (city=="FORT MADISON"|city=="KEOKUK") & state=="IA" & county=="."
qui replace county = "LINN" if city=="CEDAR RAPIDS" & state=="IA" & county=="."
qui replace county = "LUCAS" if city=="CHARITON" & state=="IA" & county=="."
qui replace county = "LYON" if city=="ROCK RAPIDS" & state=="IA" & county=="."
qui replace county = "MADISON" if city=="WINTERSET" & state=="IA" & county=="."
qui replace county = "MAHASKA" if city=="OSKALOOSA" & state=="IA" & county=="."
qui replace county = "MAHASKA" if county=="OSKALOOSA" & state=="IA"
qui replace county = "MARION" if (city=="KNOXVILLE"|city=="PELLA") & state=="IA" & county=="."
qui replace county = "MARSHALL" if city=="MARSHALLTOWN" & state=="IA" & county=="."
qui replace county = "MITCHELL" if city=="OSAGE" & state=="IA" & county=="."
qui replace county = "MONONA" if city=="ONAWA" & state=="IA" & county=="."
qui replace county = "MONTGOMERY" if city=="RED OAK" & state=="IA" & county=="."
qui replace county = "MUSCATINE" if city=="MUSCATINE" & state=="IA" & county=="."
qui replace county = "OBRIEN" if county=="O BRIEN" & state=="IA"
qui replace county = "OSCEOLA" if city=="SIBLEY" & state=="IA" & county=="."
qui replace county = "PAGE" if city=="CLARINDA" & state=="IA" & county=="."
qui replace county = "POCAHONTAS" if city=="POCAHONTAS" & state=="IA" & county=="."
qui replace county = "POLK" if city=="DES MOINES" & state=="IA" & county=="."
qui replace county = "POWESHIEK" if city=="GRINNELL" & state=="IA" & county=="."
qui replace county = "POWESHIEK" if county=="POWESHEIK" & state=="IA"
qui replace county = "RINGGOLD" if city=="MOUNT AYR" & state=="IA" & county=="."
qui replace county = "SAC" if city=="SAC CITY" & state=="IA" & county=="."
qui replace county = "SHELBY" if city=="HARLAN" & state=="IA" & county=="."
qui replace county = "SIOUX" if (city=="HAWARDEN"|city=="SIOUX CENTER") & state=="IA" & county=="."
qui replace county = "SIOUX" if county=="ROCK" & state=="IA"
qui replace county = "STORY" if city=="AMES" & state=="IA" & county=="."
qui replace county = "UNION" if city=="CRESTON" & state=="IA" & county=="."
qui replace county = "VAN BUREN" if city=="KEOSAUQUA" & state=="IA" & county=="."
qui replace county = "WAPELLO" if city=="OTTUMWA" & state=="IA" & county=="."
qui replace county = "WAPELLO" if county=="WAPELLA" & state=="IA"
qui replace county = "WASHINGTON" if city=="WASHINGTON" & state=="IA" & county=="."
qui replace county = "WEBSTER" if city=="FORT DODGE" & state=="IA" & county=="."
qui replace county = "WINNESHIEK" if city=="DECORAH" & state=="IA" & county=="."
qui replace county = "WRIGHT" if (city=="BELMOND"|city=="CLARION") & state=="IA" & county=="."

* Idaho 
qui replace county = "ADA" if city=="BOISE" & state=="ID" & county=="."
qui replace county = "ADA" if county=="ADARIS" & state=="ID"
qui replace county = "BANNOCK" if county=="BANNOCKLLS" & state=="ID"
qui replace county = "BINGHAM" if county=="BLACKFOOT" & state=="ID"
qui replace county = "BONNEVILLE" if city=="IDAHO FALLS" & state=="ID" & county=="."
qui replace county = "BONNEVILLE" if county=="BONNIEVILLE" & state=="ID"
qui replace county = "CLEARWATER" if city=="OROFINO" & state=="ID" & county=="."
qui replace county = "GOODING" if city=="GOODING" & state=="ID" & county=="."
qui replace county = "NEZ PERCE" if (county=="NEZ PERSE"|county=="NEZ") & state=="ID"

* Illinois 
qui replace county = "BOONE" if city=="BELVIDERE" & state=="IL" & county=="."
qui replace county = "BUREAU" if city=="SPRING VALLEY" & state=="IL" & county=="."
qui replace county = "CHRISTIAN" if county=="CHRISTAIN" & state=="IL"
qui replace county = "CLINTON" if county=="CILNTON" & state=="IL"
qui replace county = "COLES" if (city=="MATOON"|city=="MATTOON") & state=="IL" & county=="."
qui replace county = "COOK" if (city=="BARRINGTON"|city=="CHICAGO"|city=="ELK GROVE VILLAGE"|city=="EVANSTON"|city=="EVERGREEN PARK"|city=="HAZELCREST"|city=="HOFFMAN ESTATES"|city=="LEMONT"|city=="MELROSE PARK"|city=="OAK LAWN"|city=="OAK PARK"|city=="PALOS HEIGHTS"|city=="PARK RIDGE"|city=="SKOKIE"|city=="STREAMWOOD") & state=="IL" & county=="."
qui replace county = "COOK" if county==" P" & state=="IL"
qui replace county = "DE KALB" if county=="DEKALB" & state=="IL"
qui replace county = "DE WITT" if county=="DEWITT" & state=="IL"
qui replace county = "DU PAGE" if (city=="DOWNERS GROVE"|city=="NAPERVILLE"|city=="WINFIELD") & state=="IL" & county=="."
qui replace county = "DU PAGE" if county=="DUPAGE" & state=="IL"
qui replace county = "FORD" if county=="GIBSON" & state=="IL"
qui replace county = "GRUNDY" if city=="MORRIS" & state=="IL" & county=="."
qui replace county = "HENRY" if city=="KEWANEE" & state=="IL" & county=="."
qui replace county = "KANKAKEE" if city=="KANKAKEE" & state=="IL" & county=="."
qui replace county = "LA SALLE" if city=="STREATOR" & state=="IL" & county=="."
qui replace county = "LA SALLE" if county=="LASALLE" & state=="IL"
qui replace county = "LAKE" if (city=="LAKE BLUFF"|city=="LAKE FOREST") & state=="IL" & county=="."
qui replace county = "LOGAN" if city=="LINCOLN" & state=="IL" & county=="."
qui replace county = "MACON" if city=="DECATUR" & state=="IL" & county=="."
qui replace county = "MACON" if county=="3883" & state=="IL"
qui replace county = "MADISON" if city=="ALTON" & state=="IL" & county=="."
qui replace county = "MASSAC" if city=="METROPOLIS" & state=="IL" & county=="."
qui replace county = "MC DONOUGH" if county=="MCDONOUGH" & state=="IL"
qui replace county = "MC HENRY" if (county=="MCHENRY"|county=="MCGENRY") & state=="IL"
qui replace county = "MC LEAN" if county=="MCLEAN" & state=="IL"
qui replace county = "PERRY" if city=="PINCKNEYVILLE" & state=="IL" & county=="."
qui replace county = "SANGAMON" if city=="SPRINGFIELD" & state=="IL" & county=="."
qui replace county = "SANGAMON" if county=="SANGMON" & state=="IL"
qui replace county = "ST. CLAIR" if (city=="BELLEVILLE"|city=="EAST ST. LOUIS") & state=="IL" & county=="."
qui replace county = "TAZEWELL" if (county=="TAZWELL"|county=="TAWELL") & state=="IL"
qui replace county = "VERMILION" if county=="VERMILLION" & state=="IL"
qui replace county = "WABASH" if (city=="MT CARMEL"|city=="MT. CARMEL") & state=="IL" & county=="."
qui replace county = "WASHINGTON" if city=="NASHVILLE" & state=="IL" & county=="."
qui replace county = "WHITESIDE" if city=="STERLING" & state=="IL" & county=="."
qui replace county = "WINNEBAGO" if city=="ROCKFORD" & state=="IL" & county=="."
qui replace county = "WINNEBAGO" if county=="WINNEBAGE" & state=="IL"

* Indiana 
qui replace county = "ADAMS" if county=="ADMAS" & state=="IN"
qui replace county = "ALLEN" if (city=="FORT WAYNE"|city=="FT WAYNE") & state=="IN" & county=="."
qui replace county = "BARTHOLOMEW" if city=="COLUMBUS" & state=="IN" & county=="."
qui replace county = "BARTHOLOMEW" if county=="BARTHOLOWMEW" & state=="IN"
qui replace county = "BOONE" if city=="LEBANON" & state=="IN" & county=="."
qui replace county = "CLARK" if (city=="CHARLESTOWN"|city=="JEFFERSONVILLE") & state=="IN" & county=="."
qui replace county = "CLARK" if county=="INDIANA" & state=="IN"
qui replace county = "CLAY" if city=="BRAZIL" & state=="IN" & county=="."
qui replace county = "DE KALB" if county=="DEKALB" & state=="IN"
qui replace county = "DELAWARE" if county=="DELEWARE" & state=="IN"
qui replace county = "FRANKLIN" if county=="B" & state=="IN"
qui replace county = "GIBSON" if city=="PRINCETON" & state=="IN" & county=="."
qui replace county = "HANCOCK" if city=="GREENFIELD" & state=="IN" & county=="."
qui replace county = "HENRY" if county=="IN" & city=="NEW CASTLE" & state=="IN"
qui replace county = "HOWARD" if city=="KOKOMO" & state=="IN" & county=="."
qui replace county = "JENNINGS" if city=="NORTH VERNON" & state=="IN" & county=="."
qui replace county = "JOHNSON" if city=="GREENWOOD" & state=="IN" & county=="."
qui replace county = "LA PORTE" if (city=="LAPORTE"|city=="MICHIGAN CITY") & state=="IN" & county=="."
qui replace county = "LA PORTE" if county=="LAPORTE" & state=="IN"
qui replace county = "LAGRANGE" if city=="LAGRANGE" & state=="IN" & county=="."
qui replace county = "LAKE" if (city=="CROWN POINT"|city=="HAMMOND"|city=="HOBART"|city=="MUNSTER") & state=="IN" & county=="."
qui replace county = "LAWRENCE" if county=="LAWERNCE" & state=="IN"
qui replace county = "MARION" if city=="INDIANAPOLIS" & state=="IN" & county=="."
qui replace county = "MARION" if county=="MARION/HAMILTON" & state=="IN"
qui replace county = "MARSHALL" if (county=="MARSHAL"|county=="MARHSALL") & state=="IN"
qui replace county = "MONROE" if city=="BLOOMINGTON" & state=="IN" & county=="."
qui replace county = "MONTGOMERY" if city=="CRAWFORDSVILLE" & state=="IN" & county=="."
qui replace county = "PERRY" if county=="PERY" & state=="IN"
qui replace county = "PORTER" if county=="P" & state=="IN"
qui replace county = "RUSH" if county=="RUSHVILLE" & state=="IN"
qui replace county = "SHELBY" if county=="IN" & city=="SHELBYVILLE" & state=="IN"
qui replace county = "ST. JOSEPH" if (city=="GRANGER"|city=="MISHAWAKA"|city=="SOUTH BEND") & state=="IN" & county=="."
qui replace county = "ST. JOSEPH" if (county=="ST. JOSEPH S"|county=="WAYNEST. JOSEPH"|county=="ST. CLAIR"|county=="S") & state=="IN"
qui replace county = "STEUBEN" if county=="STEUBAN" & state=="IN"
qui replace county = "TIPPECANOE" if city=="LAFAYETTE" & state=="IN" & county=="."
qui replace county = "TIPPECANOE" if county=="TIPPEECANOE" & state=="IN"
qui replace county = "VANDERBURGH" if (county=="VANDENBURGH"|county=="VANDERBURG"|county=="VANDERBRUGH"|county=="VABDERBURGH") & state=="IN"
qui replace county = "VANDERBURGH" if city=="EVANSVILLE" & state=="IN" & county=="."
qui replace county = "VERMILLION" if city=="CLINTON" & state=="IN" & county=="."
qui replace county = "VIGO" if city=="TERRE HAUTE" & state=="IN" & county=="."
qui replace county = "WABASH" if county=="WABASJ" & state=="IN"
qui replace county = "WARREN" if city=="WILLIAMSPORT" & state=="IN" & county=="."
qui replace county = "WARRICK" if city=="NEWBURGH" & state=="IN" & county=="."
qui replace county = "WASHINGTON" if city=="SALEM" & state=="IN" & county=="."

* Kansas 
qui replace county = "ATCHISON" if city=="ATCHISON" & state=="KS" & county=="."
qui replace county = "BARBER" if (city=="KIOWA"|city=="MEDICINE LODGE") & state=="KS" & county=="."
qui replace county = "BARTON" if city=="GREAT BEND" & state=="KS" & county=="."
qui replace county = "BROWN" if (city=="HIAWATHA"|city=="HORTON"|city=="SABETHA") & state=="KS" & county=="."
qui replace county = "BROWN" if county=="BROWON" & state=="KS"
qui replace county = "BUTLER" if city=="EL DORADO" & state=="KS" & county=="."
qui replace county = "BUTLER" if county=="BULTER" & state=="KS"
qui replace county = "CHAUTAUQUA" if (city=="CEDAR VALE"|city=="SEDAN") & state=="KS" & county=="."
qui replace county = "CHAUTAUQUA" if (county=="CHAUTTAUQUA"|county=="CHAUTAUGUA") & state=="KS"
qui replace county = "CHEROKEE" if city=="COLUMBUS" & state=="KS" & county=="."
qui replace county = "CHEYENNE" if city=="ST FRANCIS" & state=="KS" & county=="."
qui replace county = "CLARK" if city=="MINNEOLA" & state=="KS" & county=="."
qui replace county = "CLAY" if city=="CLAY CENTER" & state=="KS" & county=="."
qui replace county = "CLOUD" if city=="CONCORDIA" & state=="KS" & county=="."
qui replace county = "COFFEY" if city=="BURLINGOTN" & state=="KS" & county=="."
qui replace county = "COWLEY" if (city=="ARKANSAS CITY"|city=="ARKANSAS"|city=="WINFIELD") & state=="KS" & county=="."
qui replace county = "CRAWFORD" if (city=="GIRARD"|city=="PITTSBURG") & state=="KS" & county=="."
qui replace county = "DECATUR" if city=="OBERLIN" & state=="KS" & county=="."
qui replace county = "DICKINSON" if city=="HERINGTON" & state=="KS" & county=="."
qui replace county = "DICKINSON" if county=="DICKENSON" & state=="KS"
qui replace county = "DOUGLAS" if city=="LAWRENCE" & state=="KS" & county=="."
qui replace county = "EDWARDS" if city=="KINSLEY" & state=="KS" & county=="."
qui replace county = "ELLIS" if city=="HAYS" & state=="KS" & county=="."
qui replace county = "ELLSWORTH" if city=="ELLSWORTH" & state=="KS" & county=="."
qui replace county = "FINNEY" if city=="GARDEN CITY" & state=="KS" & county=="."
qui replace county = "FRANKLIN" if city=="OTTAWA" & state=="KS" & county=="."
qui replace county = "GEARY" if city=="JUNCTION CITY" & state=="KS" & county=="."
qui replace county = "GOVE" if city=="QUINTER" & state=="KS" & county=="."
qui replace county = "GRAHAM" if city=="HILL CITY" & state=="KS" & county=="."
qui replace county = "GRANT" if city=="ULYSSES" & state=="KS" & county=="."
qui replace county = "GREELEY" if city=="TRIBUNE" & state=="KS" & county=="."
qui replace county = "GREENWOOD" if city=="EUREKA" & state=="KS" & county=="."
qui replace county = "HAMILTON" if city=="SYRACUSE" & state=="KS" & county=="."
qui replace county = "HARPER" if (city=="ANTHONY"|city=="HARPER") & state=="KS" & county=="."
qui replace county = "HARPER" if county=="ANTHONY" & state=="KS"
qui replace county = "HARVEY" if (city=="HALSTEAD"|city=="NEWTON") & state=="KS" & county=="."
qui replace county = "HASKELL" if city=="SATANTA" & state=="KS" & county=="."
qui replace county = "HODGEMAN" if city=="JETMORE" & state=="KS" & county=="."
qui replace county = "JACKSON" if city=="HOLTON" & state=="KS" & county=="."
qui replace county = "JEWELL" if city=="MANKATO" & state=="KS" & county=="."
qui replace county = "JOHNSON" if (city=="LEAWOOD"|city=="LENEXA"|city=="OLATHE"|city=="OVERLAND PARK") & state=="KS" & county=="."
qui replace county = "KEARNY" if city=="LAKIN" & state=="KS" & county=="."
qui replace county = "KINGMAN" if city=="KINGMAN" & state=="KS" & county=="."
qui replace county = "KIOWA" if city=="GREENSBURG" & state=="KS" & county=="."
qui replace county = "LABETTE" if city=="PARSONS" & state=="KS" & county=="."
qui replace county = "LANE" if city=="DIGHTON" & state=="KS" & county=="."
qui replace county = "LEAVENWORTH" if city=="LEAVENWORTH" & state=="KS" & county=="."
qui replace county = "LEAVENWORTH" if county=="LEVENWORTH" & state=="KS"
qui replace county = "LINCOLN" if city=="LINCOLN" & state=="KS" & county=="."
qui replace county = "LYON" if city=="EMPORIA" & state=="KS" & county=="."
qui replace county = "MARION" if (city=="HILLSBORO"|city=="MARION") & state=="KS" & county=="."
qui replace county = "MARSHALL" if city=="MARYSVILLE" & state=="KS" & county=="."
qui replace county = "MCPHERSON" if (city=="LINDSBORG"|city=="MOUNDRIDGE") & state=="KS" & county=="."
qui replace county = "MEADE" if city=="MEADE" & state=="KS" & county=="."
qui replace county = "MIAMI" if (city=="OSAWATOMIE"|city=="PAOLA") & state=="KS" & county=="."
qui replace county = "MITCHELL" if city=="BELOIT" & state=="KS" & county=="."
qui replace county = "MITCHELL" if county=="MITHCELL" & state=="KS"
qui replace county = "MONTGOMERY" if city=="COFFEYVILLE" & state=="KS" & county=="."
qui replace county = "MONTGOMERY" if county=="MONTGMERY" & state=="KS"
qui replace county = "MORRIS" if city=="COUNCIL GROVE" & state=="KS" & county=="."
qui replace county = "MORTON" if city=="ELKHART" & state=="KS" & county=="."
qui replace county = "NEMAHA" if city=="SENECA" & state=="KS" & county=="."
qui replace county = "NEOSHO" if city=="CHANUTE" & state=="KS" & county=="."
qui replace county = "NESS" if (city=="NESS CITY"|city=="RANSOM") & state=="KS" & county=="."
qui replace county = "NORTON" if city=="NORTON" & state=="KS" & county=="."
qui replace county = "OSBORNE" if city=="OSBORNE" & state=="KS" & county=="."
qui replace county = "OTTAWA" if city=="MINNEAPOLIS" & state=="KS" & county=="."
qui replace county = "PAWNEE" if city=="LARNED" & state=="KS" & county=="."
qui replace county = "PHILLIPS" if city=="PHILLIPSBURG" & state=="KS" & county=="."
qui replace county = "POTTAWATOMIE" if (city=="ONAGA"|city=="WAMEGO") & state=="KS" & county=="."
qui replace county = "POTTAWATOMIE" if county=="ONAGA" & state=="KS"
qui replace county = "PRATT" if city=="PRATT" & state=="KS" & county=="."
qui replace county = "RAWLINS" if city=="ATWOOD" & state=="KS" & county=="."
qui replace county = "RENO" if city=="HUTCHINSON" & state=="KS" & county=="."
qui replace county = "REPUBLIC" if city=="BELLEVILLE" & state=="KS" & county=="."
qui replace county = "RICE" if city=="LYONS" & state=="KS" & county=="."
qui replace county = "RILEY" if city=="MANHATTAN" & state=="KS" & county=="."
qui replace county = "RUSH" if city=="LA CROSSE" & state=="KS" & county=="."
qui replace county = "RUSH" if county=="LACROSSE" & state=="KS"
qui replace county = "RUSSELL" if city=="RUSSELL" & state=="KS" & county=="."
qui replace county = "SALINE" if city=="SALINA" & state=="KS" & county=="."
qui replace county = "SCOTT" if city=="SCOTT CITY" & state=="KS" & county=="."
qui replace county = "SEDGWICK" if (county=="SEDGEWICK"|county=="SEDWICK"|county=="SEGWICK") & state=="KS"
qui replace county = "SEDGWICK" if city=="WICHITA" & state=="KS" & county=="."
qui replace county = "SHAWNEE" if city=="TOPEKA" & state=="KS" & county=="."
qui replace county = "SHERIDAN" if city=="HOXIE" & state=="KS" & county=="."
qui replace county = "SHERMAN" if city=="GOODLAND" & state=="KS" & county=="."
qui replace county = "SMITH" if city=="SMITH CENTER" & state=="KS" & county=="."
qui replace county = "STAFFORD" if city=="STAFFORD" & state=="KS" & county=="."
qui replace county = "STANTON" if city=="JOHNSON" & state=="KS" & county=="."
qui replace county = "STEVENS" if city=="HUGOTON" & state=="KS" & county=="."
qui replace county = "SUMNER" if (city=="CALDWELL"|city=="WELLINGTON") & state=="KS" & county=="."
qui replace county = "THOMAS" if city=="COLBY" & state=="KS" & county=="."
qui replace county = "TREGO" if city=="WAKEENEY" & state=="KS" & county=="."
qui replace county = "WASHINGTON" if (city=="HANOVER"|city=="WASHINGTON") & state=="KS" & county=="."
qui replace county = "WICHITA" if county=="WICHIAT" & state=="KS"
qui replace county = "WILSON" if (city=="FREDONIA"|city=="NEODESHA") & state=="KS" & county=="."
qui replace county = "WYANDOTTE" if city=="KANSAS CITY" & state=="KS" & county=="."

* Kentucky 
qui replace county = "BOYD" if city=="ASHLAND" & state=="KY" & county=="."
qui replace county = "BOYLE" if city=="DANVILLE" & state=="KY" & county=="."
qui replace county = "BRECKINRIDGE" if city=="HARDINSBURG" & state=="KY" & county=="."
qui replace county = "CUMBERLAND" if city=="BURKESVILLE" & state=="KY" & county=="."
qui replace county = "DAVIESS" if city=="OWENSBORO" & state=="KY" & county=="."
qui replace county = "FAYETTE" if city=="LEXINGTON" & state=="KY" & county=="."
qui replace county = "FAYETTE" if county=="FATETTE" & state=="KY"
qui replace county = "FLOYD" if (city=="MCDOWELL"|city=="PRESTONSBURG") & state=="KY" & county=="."
qui replace county = "GRAVES" if city=="MAYFIELD" & state=="KY" & county=="."
qui replace county = "GREEN" if city=="GREENSBURG" & state=="KY" & county=="."
qui replace county = "GREEN" if county=="GREENVILLE" & state=="KY"
qui replace county = "HARRISON" if city=="CYNTHIANA" & state=="KY" & county=="."
qui replace county = "HARRISON" if county=="HARRISION" & state=="KY"
qui replace county = "HART" if county=="CAVERNA" & state=="KY"
qui replace county = "HOPKINS" if county=="HOPKINSVILLE" & state=="KY"
qui replace county = "JEFFERSON" if (city=="LOUISVILLE"|city=="LOUSIVILLE") & state=="KY" & county=="."
qui replace county = "KENTON" if (city=="COVINGTON"|city=="EDGEWOOD") & state=="KY" & county=="."
qui replace county = "LAUREL" if city=="LONDON" & state=="KY" & county=="."
qui replace county = "LINCOLN" if county=="LINCOLN         LINCOLN" & state=="KY"
qui replace county = "LOGAN" if city=="RUSSELLVILLE" & state=="KY" & county=="."
qui replace county = "MARION" if city=="LEBANON" & state=="KY" & county=="."
qui replace county = "MARSHALL" if county=="MARHSALL" & state=="KY"
qui replace county = "MASON" if city=="MAYSVILLE" & state=="KY" & county=="."
qui replace county = "MC CRACKEN" if (county=="MCCRACKEN"|county=="MCCRAKEN") & state=="KY"
qui replace county = "MC CRACKEN" if city=="PADUCAH" & state=="KY" & county=="."
qui replace county = "MERCER" if city=="HARRODSBURG" & state=="KY" & county=="."
qui replace county = "MORGAN" if city=="WEST LIBERTY" & state=="KY" & county=="."
qui replace county = "MUHLENBERG" if county=="MUHLENBURG" & state=="KY"
qui replace county = "PERRY" if city=="HAZARD" & state=="KY" & county=="."
qui replace county = "PULASKI" if city=="SOMERSET" & state=="KY" & county=="."
qui replace county = "ROCKCASTLE" if city=="MT. VERNON" & state=="KY" & county=="."
qui replace county = "SHELBY" if city=="SHELBYVILLE" & state=="KY" & county=="."
qui replace county = "TAYLOR" if city=="CAMPBELLSVILLE" & state=="KY" & county=="."
qui replace county = "TAYLOR" if county=="T" & state=="KY"
qui replace county = "TRIGG" if city=="CADIZ" & state=="KY" & county=="."
qui replace county = "UNION" if county=="UNIOIN" & state=="KY"
qui replace county = "WARREN" if city=="BOWLING GREEN" & state=="KY" & county=="."

* Louisiana 
qui replace county = "ACADIA" if (city=="CHURCH POINT"|city=="CROWLEY"|city=="EUNICE") & state=="LA" & county=="."
qui replace county = "ACADIA" if county=="ACADIAAVIS" & state=="LA"
qui replace county = "ALLEN" if city=="OAKDALE" & state=="LA" & county=="."
qui replace county = "ASCENSION" if (city=="GONZALES"|city=="GONZLAES") & state=="LA" & county=="."
qui replace county = "ASCENSION" if (county=="ACENSION"|county=="EAST. ASCENSION") & state=="LA"
qui replace county = "AVOYELLES" if (city=="BUNKIE"|city=="MARKSVILLE") & state=="LA" & county=="."
qui replace county = "BEAUREGARD" if city=="DERIDDER" & state=="LA" & county=="."
qui replace county = "BIENVILLE" if city=="ARCADIA" & state=="LA" & county=="."
qui replace county = "BIENVILLE" if county=="BEINVILLE" & state=="LA"
qui replace county = "BOSSIER" if city=="BOSSIER CITY" & state=="LA" & county=="."
qui replace county = "CADDO" if (county=="CADO"|county=="SHREVEPORT") & state=="LA"
qui replace county = "CADDO" if city=="SHREVEPORT" & state=="LA" & county=="."
qui replace county = "CALCASIEU" if city=="LAKE CHARLES" & state=="LA" & county=="."
qui replace county = "CALCASIEU" if county=="CACASIEU" & state=="LA"
qui replace county = "CAMERON" if city=="CAMERON" & state=="LA" & county=="."
qui replace county = "CLAIBORNE" if county=="CLAIBOURNE" & state=="LA"
qui replace county = "CONCORDIA" if city=="FERRIDAY" & state=="LA" & county=="."
qui replace county = "DE SOTO" if city=="STONEWALL" & state=="LA" & county=="."
qui replace county = "DE SOTO" if county=="DESOTO" & state=="LA"
qui replace county = "E. BATON ROUGE" if (county=="EBR"|county=="EAST. BATON ROUGE"|county=="E BATON ROUGE"|county=="EAST. BARON ROUGE"|county=="BATON ROUGE"|county=="ASCEBSUIB") & state=="LA"
qui replace county = "E. BATON ROUGE" if city=="BATON ROUGE" & state=="LA" & county=="."
qui replace county = "EAST CARROLL" if city=="LAKE PROVIDENCE" & state=="LA" & county=="."
qui replace county = "EAST FELICIANA" if (city=="JACKOSN"|city=="JACKSON") & state=="LA" & county=="."
qui replace county = "EAST FELICIANA" if (county=="EAST. CARROLL"|county=="EAST. FELICIANA"|county=="EAST. FELICIAN"|county=="EASE FELICIAN") & state=="LA"
qui replace county = "EVANGELINE" if city=="VILLE PLATTE" & state=="LA" & county=="."
qui replace county = "IBERIA" if city=="NEW IBERIA" & state=="LA" & county=="."
qui replace county = "JEFFERSON" if (city=="KENNER"|city=="MARRERO"|city=="METAIRIE"|city=="METARIE") & state=="LA" & county=="."
qui replace county = "JEFFERSON" if city=="METAIRIE" & state=="LA" & county=="UNKNOWN"
qui replace county = "JEFFERSON" if (county=="JEFFERSONPARISH"|county=="JEFFRSON"|county=="WEST. JEFFERSON") & state=="LA"
qui replace county = "JEFFRSON DAVIS" if (city=="JENNINGS"|city=="WELSH") & state=="LA" & county=="."
qui replace county = "JEFFRSON DAVIS" if (county=="JEFF DAVIS"|county=="JEFFERSON DAVIS") & state=="LA"
qui replace county = "LA SALLE" if county=="LASALLE" & state=="LA"
qui replace county = "LAFAYETTE" if city=="LAFAYETTE" & state=="LA" & county=="."
qui replace county = "LAFAYETTE" if county=="LAFATETTE" & state=="LA"
qui replace county = "LAFOURCHE" if city=="RACELAND" & state=="LA" & county=="."
qui replace county = "LAFOURCHE" if county=="LAFOUCHE" & state=="LA"
qui replace county = "LIVINGSTON" if city=="DENHAM SPRINGS" & state=="LA" & county=="."
qui replace county = "NATCHITOCHES" if city=="NATCHITOCHES" & state=="LA" & county=="."
qui replace county = "ORLEANS" if (county=="EAST. NEW ORLEANS"|county=="NEW ORLEANS") & state=="LA"
qui replace county = "ORLEANS" if city=="NEW ORLEANS" & state=="LA" & county=="."
qui replace county = "OUACHITA" if (county=="QUACHITA"|county=="QOACHITA"|county=="OUCHITA"|county=="OUACHTIA"|county=="OOACHITA") & state=="LA"
qui replace county = "OUACHITA" if city=="MONROE" & state=="LA" & county=="."
qui replace county = "RAPIDES" if (county=="RAIPES"|county=="GRETNALEANS"|county=="RAPIDS"|county=="RAPEIDS") & state=="LA"
qui replace county = "RAPIDES" if city=="ALEXANDRIA" & state=="LA" & county=="."
qui replace county = "RICHLAND" if city=="RAYVILLE" & state=="LA" & county=="."
qui replace county = "ST. BERNARD" if (county=="ST. BERN-ORLEANS-JEFFERSON"|county=="ST. BERNARDPARISH"|county=="ST. BERNARD & ORLEANS"|county=="ST. BERNARD & ORLEANS & JEFFERSON"|county=="ST. BERNARD & NEW ORLEANS"|city=="CHALMETTE"|city=="CHLAMETTE") & state=="LA"
qui replace county = "ST. CHARLES" if (county=="ST.  CHARLES"|county=="ST.CHARLES") & state=="LA"
qui replace county = "ST. CHARLES" if city=="LULING" & state=="LA" & county=="."
qui replace county = "ST. HELENA" if city=="GREENSBURG" & state=="LA" & county=="."
qui replace county = "ST. JAMES" if city=="LUTCHER" & state=="LA" & county=="."
qui replace county = "ST. JOHN BAPTIST" if county=="ST. JOHN THE BAPTIST" & state=="LA"
qui replace county = "ST. LANDRY" if city=="OPELOUSAS" & state=="LA" & county=="."
qui replace county = "ST. LANDRY" if county=="ST.LANDRY" & state=="LA"
qui replace county = "ST. MARY" if (city=="FRANKLIN"|city=="MORGAN CITY") & state=="LA" & county=="."
qui replace county = "ST. MARY" if (county=="ST.MARY"|county=="ST. MARYS") & state=="LA"
qui replace county = "ST. TAMMANY" if (city=="COVINGTON"|city=="MANDEVILE"|city=="MANDEVILLE") & state=="LA" & county=="."
qui replace county = "ST. TAMMANY" if (county=="MANDEVILLE"|county=="ST.TAMMY"|county=="ST.TAMMANY"|county=="ST. TAMMY") & state=="LA"
qui replace county = "TANGIPAHOA" if (city=="AMITE"|city=="HAMMOND") & state=="LA" & county=="."
qui replace county = "TANGIPAHOA" if (county=="TANGIAHOA"|county=="TANGIPAHOE") & state=="LA"
qui replace county = "TERREBONNE" if (county=="TERREBONE"|county=="TERREBORNE") & state=="LA"
qui replace county = "TERREBONNE" if city=="HOUMA" & state=="LA" & county=="."
qui replace county = "UNION" if city=="BERNICE" & state=="LA" & county=="."
qui replace county = "VERMILION" if city=="ABBEVILLE" & state=="LA" & county=="."
qui replace county = "VERMILION" if county=="VERMILLION" & state=="LA"
qui replace county = "VERNON" if city=="ROSEPINE" & state=="LA" & county=="."
qui replace county = "W. BATON ROUGE" if county=="WEST. BATON ROUGE" & state=="LA"
qui replace county = "WASHINGTON" if (city=="BOGALUSA"|city=="FRANKLINTON") & state=="LA" & county=="."
qui replace county = "WEBSTER" if city=="MINDEN" & state=="LA" & county=="."
qui replace county = "WEBSTER" if county=="WEBSTER  PLAZA" & state=="LA"
qui replace county = "WEST CARROLL" if county=="WEST. CARROLL" & state=="LA"
qui replace county = "WEST FELICIANA" if county=="WEST. FELICIANA" & state=="LA"
qui replace county = "WINN" if city=="WINNFIELD" & state=="LA" & county=="."

* Massachusetts 
qui replace county = "BARNSTABLE" if (county=="BARNSABLE"|county=="BARSTABLE") & state=="MA"
qui replace county = "BARNSTABLE" if city=="EAST SANDWICH" & state=="MA" & county=="."
qui replace county = "BRISTOL" if city=="TAUNTON" & state=="MA" & county=="."
qui replace county = "BRISTOL" if county=="NEW BEDFORD" & state=="MA"
qui replace county = "DUKES" if city=="OAK BLUFFS" & state=="MA" & county=="."
qui replace county = "ESSEX" if (city=="BEVERLY"|city=="NEWBURYPORT"|city=="SALEM") & state=="MA" & county=="."
qui replace county = "ESSEX" if county=="ESSEZ" & state=="MA"
qui replace county = "HAMPDEN" if (city=="HOLYOKE"|city=="LUDLOW"|city=="PALMER"|city=="SPRINGFIELD"|city=="WESTFIELD") & state=="MA" & county=="."
qui replace county = "HAMPDEN" if (county=="HANPDEN"|county=="HAMDEN") & state=="MA"
qui replace county = "HAMPSHIRE" if city=="NORTHAMPTON" & state=="MA" & county=="."
qui replace county = "MIDDLESEX" if (city=="ARLINGTON"|city=="BURLINGTON"|city=="CAMBRIDGE"|city=="CHESTNUT HILL"|city=="LOWELL"|city=="MALDEN"|city=="STONEHAM"|city=="TEWKSBURY"|city=="WINCHESTER"|city=="WOBURN") & state=="MA" & county=="."
qui replace county = "MIDDLESEX" if county=="MIDDLESEXX" & state=="MA"
qui replace county = "NORFOLK" if (city=="BRAINTREE"|city=="BROOKLINE"|city=="CANTON"|city=="NEEDHAM"|city=="NEEDHAN"|city=="QUINCY"|city=="WESTWOOD") & state=="MA" & county=="."
qui replace county = "NORFOLK" if (county=="NORFORK"|county=="NORFOLD"|county=="NO"|county=="WESTWOOD") & state=="MA"
qui replace county = "SUFFOLK" if (city=="BOSTON"|city=="BRIGHTON") & state=="MA" & county=="."
qui replace county = "SUFFOLK" if (county=="SUFFLOOK"|county=="SUFFIX"|county=="SUFOLK"|county=="SUFFLOK"|county=="NA") & state=="MA"
qui replace county = "WORCESTER" if (city=="CLINTON"|city=="GARDNER"|city=="LEOMINSTER"|city=="WEBSTER"|city=="WORCESTER") & state=="MA" & county=="."
qui replace county = "WORCESTER" if county=="WORCHESTER" & state=="MA"

* Maryland 
qui replace county = "ALLEGANY" if city=="CUMBERLAND" & state=="MD" & county=="MARYLAND"
qui replace county = "ALLEGANY" if city=="CUMBERLAND" & state=="MD" & county=="."
qui replace county = "ALLEGANY" if county=="ALLEGHENY" & state=="MD"
qui replace county = "ANNE ARUNDEL" if city=="CROWNSVILLE" & state=="MD" & county=="."
qui replace county = "BALTIMORE" if (city=="BALTIMORE"|city=="CATONSVILLE"|city=="TOWSON") & state=="MD" & county=="."
qui replace county = "BALTIMORE" if (county=="BALTIIMORE CITY"|county=="CITY OF BALTIMORE"|county=="BALTIMOIRE"|county=="BALITMORE CITY") & state=="MD"
qui replace county = "CECIL" if city=="ELKTON" & state=="MD" & county=="."
qui replace county = "DORCHESTER" if city=="CAMBRIDGE" & state=="MD" & county=="."
qui replace county = "FREDERICK" if county=="FRREDERICK" & state=="MD"
qui replace county = "HOWARD" if city=="COLUMBIA" & state=="MD" & county=="."
qui replace county = "KENT" if county=="KENT & QUEEN ANNE S" & state=="MD"
qui replace county = "MONTGOMERY" if (city=="BETHESDA"|city=="ROCKVILLE") & state=="MD" & county=="."
qui replace county = "PRINCE GEORGES" if (city=="LAUREL"|city=="TAKOMA PARK") & state=="MD" & county=="."
qui replace county = "PRINCE GEORGES" if (county=="PRINCE GEORGE S"|county=="PRINE"|county=="PRINCE GEROGES"|county=="PRINCE GEORGE"|county=="PRINCE") & state=="MD"
qui replace county = "ST. MARYS" if (county=="ST. MARY S"|county=="ST.MARY S") & state=="MD"
qui replace county = "ST. MARYS" if city=="LEONARDTOWN" & state=="MD" & county=="."
qui replace county = "WASHINGTON" if city=="HAGERSTOWN" & state=="MD" & county=="."
qui replace county = "WICOMICO" if city=="SALISBURY" & state=="MD" & county=="."

* Maine 
qui replace county = "ANDROSCOGGIN" if (county=="ANDROSCOGIN"|county=="ANDRSCOGGIN") & state=="ME"
qui replace county = "AROOSTOOK" if city=="FORT KENT" & state=="ME" & county=="."
qui replace county = "CUMBERLAND" if (city=="BRIDGTON"|city=="PORTLAND") & state=="ME" & county=="."
qui replace county = "KNOX" if city=="ROCKLAND" & state=="ME" & county=="."
qui replace county = "KNOX" if county=="KNOXBSCOT" & state=="ME"
qui replace county = "PENOBSCOT" if city=="BANGOR" & state=="ME" & county=="."
qui replace county = "PENOBSCOT" if county=="P" & state=="ME"
qui replace county = "PISCATAQUIS" if city=="GREENVILLE" & state=="ME" & county=="."
qui replace county = "PISCATAQUIS" if county=="PISCATIQUIS" & state=="ME"
qui replace county = "SOMERSET" if city=="PITTSFIELD" & state=="ME" & county=="."
qui replace county = "YORK" if city=="BIDDEFORD" & state=="ME" & county=="."

* Michigan 
qui replace county = "CALHOUN" if city=="BATTLE CREEK" & state=="MI" & county=="."
qui replace county = "CHEBOYGAN" if city=="CHEBOYGAN" & state=="MI" & county=="."
qui replace county = "CLINTON" if county=="ST. JOHNS" & state=="MI"
qui replace county = "DELTA" if city=="ESCANABA" & state=="MI" & county=="."
qui replace county = "EATON" if city=="LANSING" & state=="MI" & county=="."
qui replace county = "GENESEE" if (city=="FLINT"|city=="GRAND BLANC") & state=="MI" & county=="."
qui replace county = "GENESEE" if county=="GENESSEE" & state=="MI"
qui replace county = "GOGEBIC" if city=="IRONWOOD" & state=="MI" & county=="."
qui replace county = "GRATIOT" if city=="ALMA" & state=="MI" & county=="."
qui replace county = "HOUGHTON" if county=="HOUGGTON" & state=="MI"
qui replace county = "JACKSON" if city=="JACKSON" & state=="MI" & county=="."
qui replace county = "KALAMAZOO" if city=="KALAMAZOO" & state=="MI" & county=="."
qui replace county = "KENT" if city=="GRAND RAPIDS" & state=="MI" & (county=="UNITED"|county==".")
qui replace county = "KENT" if county=="232" & state=="MI"
qui replace county = "LAPEER" if city=="LAPEER" & state=="MI" & county=="."
qui replace county = "LENAWEE" if county=="LENWEE" & state=="MI"
qui replace county = "MACOMB" if (city=="HARRISON TOWNSHIP"|city=="MT. CLEMENS"|city=="NEW BALTIMORE"|city=="WARREN") & state=="MI" & county=="."
qui replace county = "MACOMB" if county=="MACOMB AND OAKLAND" & state=="MI"
qui replace county = "MANISTEE" if city=="MANISTEE" & state=="MI" & county=="."
qui replace county = "MECOSTA" if county=="BIG RAPIDS" & state=="MI" & county=="BIG RAPIDS"
qui replace county = "MONROE" if city=="MONROE" & state=="MI" & county=="."
qui replace county = "MONTCALM" if county=="MONTCLAIM" & state=="MI"
qui replace county = "MUSKEGON" if city=="MUSKEGON" & state=="MI" & county=="."
qui replace county = "OAKLAND" if (city=="AUBURN HILLS"|city=="COMMERCE TOWNSHIP"|city=="FARMINGTON HILLS"|city=="FERNDALE"|city=="MADISON HEIGHTS"|city=="PONTIAC"|city=="ROCHESTER") & state=="MI" & county=="."
qui replace county = "OAKLAND" if city=="SOUTHFIELD" & state=="MI" & (county=="O"|county==".")
qui replace county = "OGEMAW" if city=="WEST BRANCH" & state=="MI" & county=="."
qui replace county = "SAGINAW" if city=="SAGINAW" & state=="MI" & county=="."
qui replace county = "SANILAC" if city=="DECKERVILLE" & state=="MI" & county=="."
qui replace county = "SHIAWASSEE" if county=="SHIAWASSE" & state=="MI"
qui replace county = "ST. CLAIR" if county=="ST.  CLAIR" & state=="MI" & county=="."
qui replace county = "ST. CLAIR" if city=="PORT HURON" & state=="MI" & (county=="ST."|county=="ST.CLAIR")
qui replace county = "ST. JOSEPH" if (county=="ST. JOE"|county=="ST.  JOSEPH") & state=="MI"
qui replace county = "ST. JOSEPH" if city=="STURGIS" & state=="MI" & county=="ST."
qui replace county = "TUSCOLA" if city=="CARO" & state=="MI" & county=="."
qui replace county = "VAN BUREN" if city=="PAW PAW" & state=="MI" & county=="."
qui replace county = "VAN BUREN" if county=="VANBUREN" & state=="MI"
qui replace county = "WASHTENAW" if (city=="ANN ARBOR"|city=="CHELSEA") & state=="MI" & county=="."
qui replace county = "WAYNE" if (city=="DETROIT"|city=="GROSSE POINTE FARMS"|city=="GROSSE POINTE"|city=="LINCOLN PARK"|city=="NORTHVILLE"|city=="TAYLOR"|city=="TRENTON"|city=="WAYNE"|city=="WESTLAND"|city=="WYANDOTTE") & state=="MI" & county=="."
qui replace county = "WAYNE" if city=="DETROIT" & state=="MI" & (county=="DETROIT CITY"|county=="W")
qui replace county = "WAYNE" if county=="WAYNES" & state=="MI"

* Minnesota 
qui replace county = "ANOKA" if city=="COON RAPIDS" & state=="MN" & county=="."
qui replace county = "BELTRAMI" if city=="RED LAKE" & state=="MN" & county=="."
qui replace county = "BIG STONE" if city=="GRACEVILLE" & state=="MN" & county=="."
qui replace county = "BROWN" if city=="NEW ULM" & state=="MN" & county=="."
qui replace county = "DAKOTA" if city=="BURNSVILLE" & state=="MN" & county=="."
qui replace county = "DOUGLAS" if city=="ALEXANDRIA" & state=="MN" & county=="."
qui replace county = "HENNEPIN" if (city=="MAPLE GROVE"|city=="MINNEAPOLIS") & state=="MN" & county=="."
qui replace county = "HENNEPIN" if (county=="HENNIPEN"|county=="HENNEPOIN"|county=="HENNEPINN") & state=="MN"
qui replace county = "KITTSON" if city=="HALLOCK" & state=="MN" & county=="."
qui replace county = "LAC QUI PARLE" if city=="MADISON" & state=="MN" & county=="."
qui replace county = "MARTIN" if city=="FAIRMONT" & state=="MN" & county=="."
qui replace county = "MC LEOD" if county=="MCLEOD" & state=="MN"
qui replace county = "MURRAY" if city=="SLAYTON" & state=="MN" & county=="."
qui replace county = "OTTER TAIL" if county=="OTTERTAIL" & state=="MN"
qui replace county = "PINE" if city=="SANDSTONE" & state=="MN" & county=="."
qui replace county = "RAMSEY" if city=="ST. PAUL" & state=="MN" & county=="."
qui replace county = "RAMSEY" if county=="RAMSET" & state=="MN"
qui replace county = "ST. LOUIS" if city=="COOK" & state=="MN" & county=="."
qui replace county = "ST. LOUIS" if county=="ST.  LOUIS" & state=="MN"
qui replace county = "STEARNS" if (city=="PAYNESVILLE"|city=="SAUK CENTER") & state=="MN" & county=="."
qui replace county = "STEVENS" if city=="MORRIS" & state=="MN" & county=="."
qui replace county = "TRAVERSE" if city=="WHEATON" & state=="MN" & county=="."
qui replace county = "WABASHA" if city=="WABASHA" & state=="MN" & county=="."
qui replace county = "WATONWAN" if city=="MADELIA" & state=="MN" & county=="."
qui replace county = "WILKIN" if county=="WILKEN" & state=="MN"
qui replace county = "WRIGHT" if city=="MONTICELLO" & state=="MN" & county=="."
qui replace county = "YELLOW MEDCINE" if county=="YELLOW MEDICINE" & state=="MN"

* Missouri 
qui replace county = "ADAIR" if city=="KIRKSVILLE" & state=="MO" & county=="."
qui replace county = "BARRY" if city=="CASSVILLE" & state=="MO" & county=="."
qui replace county = "BOONE" if city=="COLUMBIA" & state=="MO" & county=="."
qui replace county = "BUCHANAN" if (county=="BUCHANNAN"|county=="ST. JOSEPH") & state=="MO"
qui replace county = "BUCHANAN" if city=="ST. JOSEPH" & state=="MO" & county=="."
qui replace county = "CHRISTIAN" if city=="SPRINGFIELD" & state=="MO" & county=="."
qui replace county = "CLAY" if (city=="EXCELSIOR SPRINGS"|city=="SMITHVILLE") & state=="MO" & county=="."
qui replace county = "CLINTON" if city=="CAMERON" & state=="MO" & county=="."
qui replace county = "COLE" if city=="JEFFERSON CITY" & state=="MO" & county=="."
qui replace county = "DENT" if city=="SALEM" & state=="MO" & county=="."
qui replace county = "GASCONADE" if city=="HERMANN" & state=="MO" & county=="."
qui replace county = "GREENE" if county=="GREEN" & state=="MO"
qui replace county = "HENRY" if city=="CLINTON" & state=="MO" & county=="."
qui replace county = "JACKSON" if (city=="INDEPENDENCE"|city=="KANSAS CITY"|city=="KNSAS CITY") & state=="MO" & county=="."
qui replace county = "JACKSON" if county=="JACKSONCOUNTY" & state=="MO"
qui replace county = "JOHNSON" if city=="OVERLAND PARK" & state=="MO" & county=="."
qui replace county = "KNOX" if county=="KNOB" & state=="MO"
qui replace county = "LACLEDE" if city=="LEBANON" & state=="MO" & county=="."
qui replace county = "LINN" if city=="BROOKFIELD" & state=="MO" & county=="."
qui replace county = "NEWTON" if city=="JOPLIN" & state=="MO" & county=="."
qui replace county = "PHELPS" if city=="ROLLA" & state=="MO" & county=="."
qui replace county = "ST. CHARLES" if (city=="LAKE ST. LOUIS"|city=="ST. CHARLES"|city=="ST. PETERS"|city=="WENTZVILLE") & state=="MO" & county=="."
qui replace county = "ST. CLAIR" if (city=="APPLETON CITY"|city=="APPLETON"|city=="OSCEOLA") & state=="MO" & county=="."
qui replace county = "ST. FRANCOIS" if (city=="BONNE TERRE"|city=="FARMINGTON") & state=="MO" & county=="."
qui replace county = "ST. LOUIS CITY" if (county=="CITY OF ST. LOUIS"|county=="ST.  LOUIS CITY"|county=="ST.  LOUIS"|county=="ST. . LOUIS"|county=="ST. LOUIS CITY MO"|county=="ST. LOUIS CTY"|county=="ST. LOUIS METROPOLITAN") & state=="MO"
qui replace county = "ST. LOUIS" if (city=="BRIDGETON"|city=="CREVE COEUR"|city=="MARYLAND HEIGHTS"|city=="SAINT LOUIS"|city=="ST LOUIS"|city=="ST. LOUIS") & state=="MO" & county=="."
qui replace county = "VERNON" if city=="NEVADA" & state=="MO" & county=="."

* Mississippi 
qui replace county = "CHICKASAW" if (city=="HOUSTON"|city=="OKOLONA") & state=="MS" & county=="."
qui replace county = "CHICKASAW" if city=="HOUSTON" & state=="MS" & county=="HOUSTON"
qui replace county = "CHICKASAW" if county=="CHICKSAW" & state=="MS"
qui replace county = "CHOCTAW" if city=="CHOCTAW" & state=="MS"
qui replace county = "DE SOTO" if county=="DESOTO" & state=="MS"
qui replace county = "HARRISON" if (city=="BILOXI"|city=="GULFPORT") & state=="MS" & county=="."
qui replace county = "HINDS" if (county=="HINNDS"|county=="INDSH") & state=="MS"
qui replace county = "HUMPHREYS" if county=="HUMPHRIES" & state=="MS"
qui replace county = "LAUDERDALE" if county=="LAUDERVILLE" & state=="MS"
qui replace county = "LINCOLN" if city=="BROOKHAVEN" & state=="MS" & county=="."
qui replace county = "MARSHALL" if city=="HOLLY SPRINGS" & state=="MS" & county=="."
qui replace county = "MONTGOMERY" if city=="WINONA" & state=="MS" & county=="."
qui replace county = "NESHOBA" if city=="PHILADELPHIA" & state=="MS"
qui replace county = "NEWTON" if city=="UNION" & state=="MS" & county=="."
qui replace county = "PANOLA" if city=="BATESVILLE" & state=="MS"
qui replace county = "PIKE" if city=="MCCOMB" & state=="MS" & county=="."
qui replace county = "PRENTISS" if (county=="PRENTICE"|county=="PRENTIC") & state=="MS"
qui replace county = "RANKIN" if (city=="BRANDON"|city=="JACKSON") & state=="MS" & county=="."
qui replace county = "SIMPSON" if county=="SIMSPON" & state=="MS"
qui replace county = "STONE" if city=="WIGGINS" & state=="MS" & county=="."
qui replace county = "STONE" if county=="39577" & state=="MS"
qui replace county = "SUNFLOWER" if county=="SOUTH SUNFLOWER" & state=="MS"
qui replace county = "TATE" if city=="SENATOBIA" & state=="MS" & county=="."
qui replace county = "TISHOMINGO" if city=="IUKA" & state=="MS" & county=="."
qui replace county = "UNION" if (county=="UNION CO."|county=="UNION CO") & state=="MS"
qui replace county = "WARREN" if city=="VICKSBURG" & state=="MS" & county=="."
qui replace county = "WILKINSON" if city=="CENTREVILLE" & state=="MS" & county=="."
qui replace county = "WILKINSON" if county=="WILKENSON" & state=="MS"

* Montana 
qui replace county = "BEAVERHEAD" if county=="JOHNSON" & state=="MT"
qui replace county = "BIG HORN" if county=="BIGHORN" & state=="MT"
qui replace county = "BLAINE" if county=="BLANE" & state=="MT"
qui replace county = "CARTER" if city=="EKALAKA" & state=="MT" & county=="."
qui replace county = "CHOUTEAU" if (city=="BIG SANDY"|city=="FORT BENTON") & state=="MT" & county=="."
qui replace county = "CHOUTEAU" if county=="CHOTEAU" & state=="MT"
qui replace county = "DANIELS" if city=="SCOBEY" & state=="MT" & county=="."
qui replace county = "DEER LODGE" if city=="ANACONDA" & state=="MT" & county=="."
qui replace county = "FERGUS" if city=="LEWISTOWN" & state=="MT" & county=="."
qui replace county = "FLATHEAD" if (city=="KALISPELL"|city=="WHITEFISH") & state=="MT" & county=="."
qui replace county = "GALLATIN" if (city=="BOZEMAN"|city=="J0RDAN"|city=="JARDAN") & state=="MT" & county=="."
qui replace county = "GLACIER" if city=="BROWNING" & state=="MT" & county=="."
qui replace county = "GRANITE" if city=="PHILLIPSBURG" & state=="MT" & county=="."
qui replace county = "LEWIS AND CLARK" if (county=="LEWIS & CLARK"|county=="LEWIS AND CLARKI") & state=="MT"
qui replace county = "LEWIS AND CLARK" if city=="HELENA" & state=="MT" & county=="."
qui replace county = "MUSSELSHELL" if county=="MUSSELLSHELL" & state=="MT"
qui replace county = "PHILLIPS" if city=="MALTA" & state=="MT" & county=="."
qui replace county = "PRAIRIE" if city=="TERRY" & state=="MT" & county=="."
qui replace county = "STILLWATER" if city=="COLUMBUS" & state=="MT" & county=="."
qui replace county = "SWEET GRASS" if county=="SWEETGRASS" & state=="MT"
qui replace county = "TETON" if city=="CHOTEAU" & state=="MT" & county=="."
qui replace county = "TOOLE" if city=="SHELBY" & state=="MT" & county=="."
qui replace county = "VALLEY" if city=="GLASGOW" & state=="MT" & county=="."
qui replace county = "YELLOWSTONE" if county=="NOT KNOWN" & city=="BILLINGS" & state=="MT"

* North Carolina 
qui replace county = "BEAUFORT" if (county=="BEUAFORT"|county=="PUNGO") & state=="NC"
qui replace county = "BRUNSWICK" if (city=="LLELAND"|city=="SUPPLY") & state=="NC" & county=="."
qui replace county = "BUNCOMBE" if (city=="ASHEVILLE"|city=="ASHVILLE") & state=="NC" & county=="."
qui replace county = "CALDWELL" if city=="LENOIR" & state=="NC" & county=="."
qui replace county = "CHOWAN" if city=="EDENTON" & state=="NC" & county=="."
qui replace county = "CLEVELAND" if city=="SHELBY" & state=="NC" & county=="."
qui replace county = "CUMBERLAND" if city=="FAYETTEVILLE" & state=="NC" & county=="."
qui replace county = "DURHAM" if city=="DURHAM" & state=="NC" & county=="."
qui replace county = "EDGECOMBE" if (county=="EDGECOME"|county=="EDGECOMB") & state=="NC"
qui replace county = "NASH" if city=="ROCKY MOUNT" & state=="NC" & county=="."  // also in EDGECOMBE county
qui replace county = "FORSYTH" if city=="WINSTON-SALEM" & state=="NC" & county=="."
qui replace county = "FORSYTH" if rpt_rc_num==270990|rpt_rc_num==271058
qui replace county = "FORSYTH" if county=="FORSYTHERY" & state=="NC"
qui replace county = "GASTON" if city=="GASTONIA" & state=="NC" & county=="."
qui replace county = "GRANVILLE" if city=="BUTNER" & state=="NC" & county=="."
qui replace county = "GUILFORD" if city=="GREENSBORO" & state=="NC" & county=="."
qui replace county = "HARNETT" if city=="ERWIN" & state=="NC" & county=="."
qui replace county = "HENDERSON" if city=="HENDERSONVILLE" & state=="NC" & county=="."
qui replace county = "IREDELL" if city=="STATESVILLE" & state=="NC" & county=="."
qui replace county = "IREDELL" if county=="IREFELL" & state=="NC"
qui replace county = "LENOIR" if city=="KINSTON" & state=="NC" & county=="."
qui replace county = "MACON" if city=="HIGHLANDS" & state=="NC" & county=="."
qui replace county = "MC DOWELL" if county=="MCDOWELL" & state=="NC"
qui replace county = "MECKLENBURG" if (city=="CHARLOTTE"|city=="PINEVILLE") & state=="NC" & county=="."
qui replace county = "MECKLENBURG" if county=="MECKLENGURG" & state=="NC"
qui replace county = "ONSLOW" if city=="JACKSONVILLE" & state=="NC" & county=="."
qui replace county = "PENDER" if city=="BURGAW" & state=="NC" & county=="."
qui replace county = "PITT" if city=="GREENVILLE" & state=="NC" & county=="."
qui replace county = "SWAIN" if city=="CHEROKEE" & state=="NC" & county=="."
qui replace county = "SWAIN" if hosp_name=="CHEROKEE INDIAN HOSPITAL" & state=="NC"
qui replace county = "SWAIN" if county=="SWAN" & state=="NC"
qui replace county = "WAKE" if (city=="GARNER"|city=="RALEIGH") & state=="NC" & county=="."

* North Dakota 
qui replace county = "CASS" if city=="FARGO" & state=="ND" & county=="."
qui replace county = "CASS" if county=="CASSNREEN" & state=="ND"
qui replace county = "MCLEAN" if city=="GARRISON" & state=="ND" & county=="."
qui replace county = "ROLETTE" if (city=="BELCOURT"|city=="ROLLA") & state=="ND" & county=="."
qui replace county = "ROLETTE" if county=="ROLLETTE" & state=="ND"
qui replace county = "SIOUX" if city=="FORT YATES" & state=="ND" & county=="."
qui replace county = "STARK" if city=="RICHARDTON" & state=="ND" & county=="."
qui replace county = "TRAILL" if city=="HILLSBORO" & state=="ND" & county=="."
qui replace county = "WALSH" if city=="GRAFTON" & state=="ND" & county=="."
qui replace county = "WARD" if city=="MINOT" & state=="ND" & county=="."

* Nebraska 
qui replace county = "ADAMS" if city=="HASTINGS" & state=="NE" & county=="."
qui replace county = "ADAMS" if county=="A" & state=="NE"
qui replace county = "ANTELOPE" if city=="NELIGH" & state=="NE" & county=="."
qui replace county = "BOONE" if city=="ALBION" & state=="NE" & county=="."
qui replace county = "BOYD" if city=="LYNCH" & state=="NE" & county=="."
qui replace county = "BUFFALO" if city=="KEARNEY" & state=="NE" & county=="."
qui replace county = "BURT" if city=="OAKLAND" & state=="NE" & county=="."
qui replace county = "BUTLER" if city=="DAVID CITY" & state=="NE" & county=="."
qui replace county = "CHASE" if city=="IMPERIAL" & state=="NE" & county=="."
qui replace county = "CHERRY" if city=="VALENTINE" & state=="NE" & county=="."
qui replace county = "CHEYENNE" if city=="SIDNEY" & state=="NE" & county=="."
qui replace county = "COLFAX" if city=="SCHUYLER" & state=="NE" & county=="."
qui replace county = "CUMING" if city=="WEST POINT" & state=="NE" & county=="."
qui replace county = "CUSTER" if (city=="BROKEN BOW"|city=="CALLAWAY"|city=="SARGENT") & state=="NE" & county=="."
qui replace county = "DAWES" if city=="CHADRON" & state=="NE" & county=="."
qui replace county = "DAWSON" if (city=="COZAD"|city=="GOTHENBURG"|city=="LEXINGTON") & state=="NE" & county=="."
qui replace county = "DOUGLAS" if city=="OMAHA" & state=="NE" & (county=="OMAHA"|county=="PAPILLION"|county==".")
qui replace county = "DOUGLAS" if county=="DOUGLASS" & state=="NE"
qui replace county = "FILLMORE" if city=="GENEVA" & state=="NE" & county=="."
qui replace county = "GAGE" if city=="BEATRICE" & state=="NE" & county=="."
qui replace county = "HALL" if city=="GRAND ISLAND" & state=="NE" & county=="."
qui replace county = "HARLAN" if city=="ALMA" & state=="NE" & county=="."
qui replace county = "HOLT" if city=="ATKINSON" & state=="NE" & county=="."
qui replace county = "JEFFERSON" if city=="FAIRBURY" & state=="NE" & county=="."
qui replace county = "KEITH" if city=="OGALLALA" & state=="NE" & county=="."
qui replace county = "KEITH" if county=="KIETH" & state=="NE"
qui replace county = "KIMBALL" if city=="KIMBALL" & state=="NE" & county=="."
qui replace county = "KNOX" if city=="CREIGHTON" & state=="NE" & county=="."
qui replace county = "LANCASTER" if city=="LINCOLN" & state=="NE" & county=="."
qui replace county = "LANCASTER" if county=="L" & state=="NE"
qui replace county = "LINCOLN" if city=="NORTH PLATTE" & state=="NE" & county=="."
qui replace county = "MADISON" if city=="NORFOLK" & state=="NE" & county=="."
qui replace county = "MADISON" if county=="M" & state=="NE"
qui replace county = "MERRICK" if city=="CENTRAL CITY" & state=="NE" & county=="."
qui replace county = "NEMAHA" if city=="AUBURN" & state=="NE" & county=="."
qui replace county = "OTOE" if city=="SYRACUSE" & state=="NE" & county=="."
qui replace county = "PAWNEE" if city=="PAWNEE CITY" & state=="NE" & county=="."
qui replace county = "PHELPS" if city=="HOLDREGE" & state=="NE" & county=="."
qui replace county = "PIERCE" if (city=="OSMOND"|city=="PLAINVIEW") & state=="NE" & county=="."
qui replace county = "PLATTE" if city=="COLUMBUS  NEBRASKA" & state=="NE" & county=="."
qui replace county = "POLK" if city=="OSCEOLA" & state=="NE" & county=="."
qui replace county = "RED WILLOW" if city=="MCCOOK" & state=="NE" & county=="."
qui replace county = "RICHARDSON" if (city=="FALLS CITY"|city=="HUMBOLDT") & state=="NE" & county=="."
qui replace county = "RICHARDSON" if county=="RICHARD" & state=="NE"
qui replace county = "SALINE" if (city=="CRETE"|city=="FRIEND") & state=="NE" & county=="."
qui replace county = "SARPY" if city=="PAPILLION" & state=="NE" & county=="."
qui replace county = "SCOTT BLUFF" if city=="SCOTTSBLUFF" & state=="NE" & county=="."
qui replace county = "SCOTT BLUFF" if county=="SCOTTS BLUFF" & state=="NE"
qui replace county = "SEWARD" if city=="SEWARD" & state=="NE" & county=="."
qui replace county = "SHERIDAN" if city=="GORDON" & state=="NE" & county=="."
qui replace county = "THAYER" if city=="HEBRON" & state=="NE" & county=="."
qui replace county = "THURSTON" if (city=="PENDER"|city=="WINNEBAGO") & state=="NE" & county=="."
qui replace county = "WASHINGTON" if city=="BLAIR" & state=="NE" & county=="."
qui replace county = "WAYNE" if city=="WAYNE" & state=="NE" & county=="."
qui replace county = "WEBSTER" if city=="RED CLOUD" & state=="NE" & county=="."
qui replace county = "YORK" if city=="YORK" & state=="NE" & county=="."

* New Hampshire 
qui replace county = "GRAFTON" if city=="LEBANON" & state=="NH" & county=="."
qui replace county = "HILLSBOROUGH" if (city=="MANCHESTER"|city=="NASHUA") & state=="NH" & county=="."
qui replace county = "HILLSBOROUGH" if county=="HILLSBORO" & state=="NH"
qui replace county = "MERRIMACK" if city=="CONCORD" & state=="NH" & county=="."
qui replace county = "ROCKINGHAM" if (city=="EXETER"|city=="HAMPSTEAD"|city=="PORTSMOUTH") & state=="NH" & county=="."
qui replace county = "STRAFFORD" if city=="DOVER" & state=="NH" & county=="."

* New Jersey 
qui replace county = "BURLINGTON" if (city=="MARLTON"|city=="MOUNT HOLLY") & state=="NJ" & county=="."
qui replace county = "BURLINGTON" if county=="MARLTON" & state=="NJ"
qui replace county = "CAMDEN" if (city=="BLACKWOOD"|city=="CAMDEN") & state=="NJ" & county=="."
qui replace county = "ESSEX" if (city=="CEDAR GROVE"|city=="IRVINGTON"|city=="LIVINGSTON"|city=="MONTCLAIR"|city=="NEWARK"|city=="ORANGE") & state=="NJ" & county=="."
qui replace county = "HUDSON" if city=="JERSEY CITY" & state=="NJ" & county=="."
qui replace county = "HUDSON" if county=="HUDON" & state=="NJ"
qui replace county = "MERCER" if city=="TRENTON" & state=="NJ" & county=="."
qui replace county = "MIDDLESEX" if (city=="NEW BRUNSWICK"|city=="PISCATAWAY") & state=="NJ" & county=="."
qui replace county = "MONMOUTH" if (city=="FREEHOLD"|city=="TINTON FALLS") & state=="NJ" & county=="."
qui replace county = "OCEAN" if city=="TOMS RIVER" & state=="NJ" & county=="."
qui replace county = "OCEAN" if county=="DOVER" & state=="NJ"
qui replace county = "PASSAIC" if (city=="PATERSON"|city=="WAYNE") & state=="NJ" & county=="."
qui replace county = "SOMERSET" if (city=="BELLE MEAD"|city=="PEAPACK") & state=="NJ" & county=="."
qui replace county = "SUSSEX" if county=="SUSSES" & state=="NJ"
qui replace county = "UNION" if (city=="SUMMIT"|city=="UNION") & state=="NJ" & county=="."
qui replace county = "UNION" if county=="SUMMIT" & state=="NJ"

* New Mexico 
qui replace county = "BERNALILLO" if (city=="ALBUEQUERQUE"|city=="ALBUQUERQUE") & state=="NM" & county=="."
qui replace county = "BERNALILLO" if (county=="EERNALILLO"|county=="BERNILLIO"|county=="BERNALLILO"|county=="BERNAILLO"|county=="BERNADILLO"|county=="BERN") & state=="NM"
qui replace county = "CHAVES" if county=="CHAVEZ" & state=="NM"
qui replace county = "CIBOLA" if city=="SAN FIDEL" & state=="NM" & county=="."
qui replace county = "CURRY" if city=="CLOVIS" & state=="NM" & county=="."
qui replace county = "DE BACA" if county=="DEBACA" & state=="NM"
qui replace county = "DONA ANA" if (county=="DON ANA"|county=="              DONA ANA") & state=="NM"
qui replace county = "DONA ANA" if city=="LAS CRUCES" & state=="NM" & county=="."
qui replace county = "EDDY" if city=="ARTESIA" & state=="NM" & county=="."
qui replace county = "LEA" if city=="HOBBS" & state=="NM" & county=="."
qui replace county = "LINCOLN" if city=="RUIDOSO" & state=="NM" & county=="."
qui replace county = "MCKINLEY" if (city=="CROWNPOINT"|city=="GALLUP"|city=="ZUNI") & state=="NM" & county=="."
qui replace county = "OTERO" if city=="MESCALERO" & state=="NM" & county=="."
qui replace county = "QUAY" if city=="TUCUMCARI" & state=="NM" & county=="."
qui replace county = "ROOSEVELT" if city=="PORTALES" & state=="NM" & county=="."
qui replace county = "SAN JUAN" if (city=="FARMINGTON"|city=="SHIPROCK") & state=="NM" & county=="."
qui replace county = "SAN JUAN" if city=="SHIPROCK" & county=="UNKNOWN" & state=="NM"
qui replace county = "SANTA FE" if (city=="ESPANOLA"|city=="SANTA FE"|city=="SANTE FE") & state=="NM" & county=="."
qui replace county = "SOCORRO" if city=="SOCORRO" & state=="NM" & county=="."

* Nevada 
qui replace county = "CLARK" if (county=="CLARKE"|county=="WHITE WINE"|county=="WHIT PINE"|county=="WHIE PINE"|county=="LAS VEGAS") & state=="NV"
qui replace county = "CLARK" if (city=="LAS VEGAS"|city=="NORTH LAS VEGAS") & state=="NV" & county=="."
qui replace county = "CLARK" if city=="LAS VEGAS" & state=="NV" & (county=="I DONT KNOW"|county=="RESERVE COST. REPORT")
qui replace county = "CARSON CITY" if (county=="CARSON"|county=="CARSONMA CITY") & state=="NV"
qui replace county = "NYE" if city=="TONOPAH" & state=="NV" & county=="."
qui replace county = "WASHOE" if (city=="INCLINE VILLAGE"|city=="RENO"|city=="SPARKS") & state=="NV" & county=="."
qui replace county = "WHITE PINE" if city=="ELY" & state=="NV" & county=="."

* New York 
qui replace county = "ALBANY" if city=="ALBANY" & state=="NY" & county=="."
qui replace county = "BRONX" if (county=="THE BRONX"|county=="BRONX NEW YORK"|county=="BRONK") & state=="NY"
qui replace county = "BRONX" if city=="RIVERDALE" & state=="NY" & county=="."
qui replace county = "BRONX" if city=="BRONX" & state=="NY" & county=="."
qui replace county = "BROOME" if county=="COUNTY" & city=="BINGHAMTON" & state=="NY"
qui replace county = "CATTARAUGUS" if (county=="CATTARUAGUS"|county=="CATTARAGUS"|county=="CATT") & state=="NY"
qui replace county = "CATTARAUGUS" if city=="GOWANDA" & state=="NY" & county=="."
qui replace county = "CHAUTAUQUA" if city=="DUNKIRK" & state=="NY" & county=="."
qui replace county = "DELAWARE" if (city=="DELHI"|city=="SIDNEY") & state=="NY" & county=="."
qui replace county = "DELAWARE" if (county=="DELEWARE"|county=="DALWARE") & state=="NY"
qui replace county = "ERIE" if (city=="BUFFALO"|city=="CHEEKTOWAGA") & state=="NY" & county=="."
qui replace county = "ERIE" if (county=="ERIR"|county=="EIRE") & state=="NY"
qui replace county = "ESSEX" if city=="TICONDEROGA" & state=="NY" & county=="."
qui replace county = "ESSEX" if county=="UTICA" & city=="ELIZABETHTOWN" & state=="NY"
qui replace county = "FRANKLIN" if (city=="MALONE"|city=="SARANAC LAKE") & state=="NY" & county=="."
qui replace county = "FULTON" if county=="LG" & state=="NY"
qui replace county = "GENESEE" if (county=="GENNESSEE"|county=="GENESSE"|county=="GENNESEE") & state=="NY"
qui replace county = "JEFFERSON" if city=="ALEXANDRIA BAY" & state=="NY" & county=="."
qui replace county = "KINGS" if city=="BROOKLYN" & state=="NY" & (county=="."|county=="KING S")
qui replace county = "KINGS" if (county=="KING S "|county=="BROOKLYN") & state=="NY"
qui replace county = "MADISON" if (city=="HAMILTON"|city=="ONEIDA") & state=="NY" & county=="."
qui replace county = "MANHATTAN" if (county=="MANHATTANN"|county=="MANHATTEN"|county=="MAN"|county=="MAHATTAN") & state=="NY"
qui replace county = "MONROE" if (city=="BROCKPORT"|city=="ROCHESTER") & state=="NY" & county=="."
qui replace county = "MONROE" if county=="N" & state=="NY"
qui replace county = "MONTGOMERY" if city=="AMSTERDAM" & state=="NY" & county=="."
qui replace county = "NASSAU" if (city=="BETHPAGE"|city=="LONG BEACH"|city=="OCEANSIDE"|city=="SYOSSET") & state=="NY" & county=="."
qui replace county = "NASSAU" if (county=="NASSUA"|county=="NASSASU") & state=="NY"
qui replace county = "NASSAU" if zip == "11791" & county=="." & state=="NY"
qui replace county = "NEW YORK" if (city=="NEW  YORK"|city=="NEW YORK"|city=="ROOSEVELT ISLAND") & state=="NY" & county=="."
qui replace county = "NEW YORK" if (county=="MANHATTAN"|county=="NYC"|county=="NEW YORK CITY"|county=="NEW YORKY"|county=="NY"|county=="EN") & state=="NY"
qui replace county = "NIAGARA" if city=="LEWISTON" & state=="NY" & county=="."
qui replace county = "ONEIDA" if (city=="ROME"|city=="UTICA") & state=="NY" & county=="."
qui replace county = "ONEIDA" if county=="ONIEDA" & state=="NY"
qui replace county = "ONONDAGA" if (city=="SYRACURSE"|city=="SYRACUSE") & state=="NY" & county=="."
qui replace county = "ONONDAGA" if (county=="SYRACUSE"|county=="ONONODAGA"|county=="ONONDOGA"|county=="ONONADAGA"|county=="ONAONDAGA") & state=="NY"
qui replace county = "ONTARIO" if (city=="CLIFTON SPRINGS"|city=="GENEVA") & state=="NY" & county=="."
qui replace county = "ORANGE" if (city=="CORNWALL"|city=="MIDDLETOWN"|city=="NEWBURGH"|city=="WARWICK") & state=="NY" & county=="."
qui replace county = "QUEENS" if (city=="ELMHURST"|city=="FAR ROCKAWAY"|city=="FLUSHING"|city=="FOREST HILLS"|city=="HOLLISWOOD"|city=="JAMAICA"|city=="REGO PARK") & state=="NY" & county=="."
qui replace county = "RENSSELAER" if (county=="RENNSELAER"|county=="RENNESLAER"|county=="RENNSALEAR") & state=="NY"
qui replace county = "RENSSELAER" if city=="TROY" & state=="NY" & county=="."
qui replace county = "RICHMOND" if city=="STATEN ISLAND" & state=="NY" & county=="."
qui replace county = "RICHMOND" if county=="STATEN ISLAND" & state=="NY"
qui replace county = "ROCKLAND" if city=="NYACK" & state=="NY" & county=="."
qui replace county = "ROCKLAND" if county=="ROCKLANDF" & state=="NY"
qui replace county = "SARATOGA" if (county=="SARATOGA SPRINGS"|county=="SARATIGA SORINGS") & state=="NY"
qui replace county = "SARATOGA" if city=="SARATOGA SPRINGS" & state=="NY" & county=="."
qui replace county = "ST. LAWRENCE" if (city=="GOUVERNEUR"|city=="MASSENA"|city=="OGDENSBURG"|city=="POTSDAM"|city=="POTSDAME"|city=="STAR LAKE") & state=="NY" & county=="."
qui replace county = "ST. LAWRENCE" if (county=="SAINT LAWRENCE"|county=="ST.LAWRENCE") & state=="NY"
qui replace county = "STEUBEN" if (city=="BATH"|city=="CORNING") & state=="NY" & county=="."
qui replace county = "STEUBEN" if county=="STUEBEN" & state=="NY"
qui replace county = "SUFFOLK" if city=="STONY BROOK" & state=="NY" & county=="."
qui replace county = "SUFFOLK" if county=="SUFFOK" & state=="NY"
qui replace county = "ULSTER" if (city=="ELLENVILLE"|city=="KINGSTON") & state=="NY" & county=="."
qui replace county = "WESTCHESTER" if (city=="BRIACLIFF MANOR"|city=="BRIARCLIFF MANOR"|city=="KATONAH"|city=="MOUNT VERNON"|city=="NEW ROCHELLE"|city=="PORT CHESTER"|city=="RYE"|city=="VALHALLA"|city=="WHITE PLAINS"|city=="YONKERS") & state=="NY" & county=="."
qui replace county = "WESTCHESTER" if (county=="WESTCHSETER"|county=="WESTHCHESTER"|county=="WESTCHETSER"|county=="WESCHESTER") & state=="NY"

* Ohio 
qui replace county = "ALLEN" if county=="LIMA" & state=="OH"
qui replace county = "BELMONT" if (city=="BELMONT"|city=="ST. CLAIRSVILLE") & state=="OH" & county=="."
qui replace county = "BROWN" if county=="OHIO" & state=="OH"
qui replace county = "CUYAHOGA" if (city=="BEACHWOOD"|city=="CLEVELAND"|city=="INDEPENDENCE") & state=="OH" & county=="."
qui replace county = "CUYAHOGA" if (county=="CUTYHOGA"|county=="C") & state=="OH"
qui replace county = "FRANKLIN" if (city=="COLUMBUS"|city=="GROVEPORT"|city=="HILLIARD") & state=="OH" & county=="."
qui replace county = "FRANKLIN" if county=="FRANKLINY" & state=="OH"
qui replace county = "FULTON" if city=="WAUSEON" & state=="OH" & county=="."
qui replace county = "GEAUGA" if city=="CHAGRIN FALLS" & state=="OH" & county=="."
qui replace county = "GUERNSEY" if county=="CAMBRIDGE" & state=="OH"
qui replace county = "HAMILTON" if city=="CINCINNATI" & state=="OH" & county=="."
qui replace county = "HANCOCK" if city=="FINDLAY" & state=="OH" & county=="."
qui replace county = "HURON" if city=="WILLARD" & state=="OH" & county=="."
qui replace county = "HURON" if county=="WILLARD" & state=="OH"
qui replace county = "JACKSON" if city=="JACKSON" & state=="OH" & county=="."
qui replace county = "LORAIN" if (city=="AMHERST"|city=="ELYRIA") & state=="OH" & county=="."
qui replace county = "LUCAS" if (city=="MAUMEE"|city=="TOLEDO") & state=="OH" & county=="."
qui replace county = "LUCAS" if (county=="MAUMEE"|county=="LUCUC"|county=="TOLEDO") & state=="OH"
qui replace county = "MADISON" if county=="M" & state=="OH"
qui replace county = "MAHONING" if (city=="YOUNGSTOWN"|city=="YOUNGTOWN") & state=="OH" & county=="."
qui replace county = "MAHONING" if county=="MOHONING" & state=="OH"
qui replace county = "MONTGOMERY" if (county=="DAYTON"|county=="MONGOMERY") & state=="OH"
qui replace county = "MONTGOMERY" if city=="DAYTON" & state=="OH" & county=="."
qui replace county = "MUSKINGUM" if city=="ZANESVILLE" & state=="OH" & county=="."
qui replace county = "RICHLAND" if (city=="MANSFIELD"|city=="MANSFILED") & state=="OH" & county=="."
qui replace county = "SCIOTO" if county=="S" & state=="OH"
qui replace county = "SENECA" if city=="TIFFIN" & state=="OH" & county=="."
qui replace county = "SENECA" if county=="TIFFIN" & state=="OH"
qui replace county = "SHELBY" if county=="SIDNEY" & state=="OH"
qui replace county = "STARK" if city=="CANTON" & state=="OH" & county=="."
qui replace county = "SUMMIT" if city=="AKRON" & state=="OH" & county=="."
qui replace county = "SUMMIT" if county=="SUMMITT" & state=="OH"
qui replace county = "TRUMBULL" if city=="WARREN" & state=="OH" & county=="."
qui replace county = "WILLIAMS" if city=="BRYAN" & state=="OH" & county=="."

* Oklahoma 
qui replace county = "ATOKA" if city=="ATOKA" & state=="OK" & county=="."
qui replace county = "BLAINE" if city=="OKEENE" & state=="OK" & county=="."
qui replace county = "CARTER" if (county=="ARDMORE"|county=="CARTERE") & state=="OK"
qui replace county = "CARTER" if city=="HEALDTON" & state=="OK" & county=="."
qui replace county = "CHEROKEE" if city=="TAHLEQUAH" & state=="OK" & county=="."
qui replace county = "CHEROKEE" if county=="TAHLEQUAH" & city=="TAHLEQUAH" & state=="OK"
qui replace county = "CHOCTAW" if city=="HUGO" & state=="OK" & county=="."
qui replace county = "CLEVELAND" if (city=="NORMAN"|city=="OKLAHOMA CITY") & state=="OK" & county=="."
qui replace county = "CLEVELAND" if county=="NORMAN" & state=="OK"
qui replace county = "COMANCHE" if (county=="COMMANCHE"|county=="CAMANCHE") & state=="OK"
qui replace county = "COMANCHE" if city=="LAWTON" & state=="OK" & county=="."
qui replace county = "DELAWARE" if county=="DELWARE" & state=="OK"
qui replace county = "GARFIELD" if city=="ENID" & state=="OK" & county=="."
qui replace county = "GARFIELD" if county=="GARFILED" & state=="OK"
qui replace county = "GARVIN" if city=="PAULS VALLEY" & state=="OK" & county=="."
qui replace county = "GRADY" if city=="CHICKASHA" & state=="OK" & county=="."
qui replace county = "HUGHES" if city=="HOLDENVILLE" & state=="OK" & county=="."
qui replace county = "JEFFERSON" if city=="WAURIKA" & state=="OK" & county=="."
qui replace county = "LE FLORE" if city=="TALIHINA" & state=="OK" & county=="."
qui replace county = "LE FLORE" if county=="LEFLORE" & state=="OK"
qui replace county = "LINCOLN" if city=="PRAGUE" & state=="OK" & county=="."
qui replace county = "LOGAN" if city=="GUTHRIE" & state=="OK" & county=="."
qui replace county = "MARSHALL" if county=="MARHSALL" & state=="OK"
qui replace county = "MUSKOGEE" if city=="MUSKOGEE" & state=="OK" & county=="."
qui replace county = "NOWATA" if city=="NOWATA" & state=="OK" & county=="."
qui replace county = "OKFUSKEE" if city=="OKEMAH" & state=="OK" & county=="."
qui replace county = "OKLAHOMA" if (city=="BETHANY"|city=="EDMUND") & state=="OK" & county=="."
qui replace county = "OKLAHOMA" if (county=="OK"|county=="OKLAHOMA CITY"|county=="OKLAMOHA"|county=="OLLAHOMA"|county=="OKLSHOMS"|county=="OKLAHMOMA CITY") & state=="OK"
qui replace county = "OKLAHOMA" if county=="TAHLEQUAH" & city=="OKLAHOMA" & state=="OK"
qui replace county = "OSAGE" if (city=="BARTESVILLE"|city=="PAWHUSKA"|city=="TULSA") & state=="OK" & county=="."
qui replace county = "OSAGE" if county=="O" & state=="OK"
qui replace county = "OTTAWA" if city=="MIAMI" & state=="OK" & county=="."
qui replace county = "OTTAWA" if county=="OWATTA" & state=="OK"
qui replace county = "PITTSBURG" if city=="MCALESTER" & state=="OK" & county=="."
qui replace county = "PITTSBURG" if county=="PITTSBURGH" & state=="OK"
qui replace county = "PONTOTOC" if (county=="PONTOC"|county=="PONTONOC") & state=="OK"
qui replace county = "PONTOTOC" if city=="ADA" & state=="OK" & county=="."
qui replace county = "POTTAWATOMIE" if city=="SHAWNEE" & state=="OK" & county=="."
qui replace county = "POTTAWATOMIE" if county=="POTAWATAMIE" & state=="OK"
qui replace county = "ROGER MILLS" if city=="CHEYENNE" & state=="OK" & county=="."
qui replace county = "ROGERS" if city=="CLAREMORE" & state=="OK" & county=="."
qui replace county = "ROGERS" if county=="RODGERS" & state=="OK"
qui replace county = "SEMINOLE" if city=="SEMINOLE" & state=="OK" & county=="."
qui replace county = "TULSA" if city=="BROKEN ARROW" & state=="OK" & county=="."
qui replace county = "TULSA" if county=="TULSAO" & state=="OK"
qui replace county = "WAGONER" if city=="WAGONER" & state=="OK" & county=="."
qui replace county = "WASHITA" if city=="CORDELL" & state=="OK" & county=="."
qui replace county = "WOODWARD" if city=="WOODWARD" & state=="OK" & county=="."

* Oregon 
qui replace county = subinstr(county," OREGON","",.) if state=="OR"
qui replace county = "BAKER" if city=="BAKER CITY" & state=="OR" & county=="."
qui replace county = "BENTON" if city=="CORVALLIS" & state=="OR" & county=="."
qui replace county = "CLACKAMAS" if county=="CLAKAMAS" & state=="OR"
qui replace county = "COOS" if (city=="BANDON"|city=="COQUILLE") & state=="OR" & county=="."
qui replace county = "CROOK" if city=="PRINEVILLE" & state=="OR" & county=="."
qui replace county = "CURRY" if city=="GOLD BEACH" & state=="OR" & county=="."
qui replace county = "DOUGLAS" if city=="ROSEBURG" & state=="OR" & county=="."
qui replace county = "HOOD RIVER" if county=="HOOD" & state=="OR"
qui replace county = "LINCOLN" if city=="NEWPORT" & state=="OR" & county=="."
qui replace county = "MARION" if city=="STAYTON" & state=="OR" & county=="."
qui replace county = "MULTNOMAH" if city=="PORTLAND" & state=="OR" & county=="."
qui replace county = "MULTNOMAH" if county=="PORTLAND" & state=="OR"
qui replace county = "TILLAMOOK" if city=="TILLAMOOK" & state=="OR" & county=="."
qui replace county = "WASHINGTON" if city=="TIGARD" & state=="OR" & county=="."
qui replace county = "YAMHILL" if city=="MCMINNVILLE" & state=="OR" & county=="."

* Pennsylvania 
qui replace county = "ALLEGHENY" if (city=="BRADDOCK"|city=="BRIDGEVILLE"|city=="MC KEES ROCKS"|city=="MCKEESPORT"|city=="MONONGAHELA"|city=="MONROEVILLE"|city=="OAKDALE"|city=="PITTSBURGH"|city=="SEWICKLEY") & state=="PA" & county=="."
qui replace county = "ALLEGHENY" if (county=="ALLEGHENEY"|county=="ALLEGHANY") & state=="PA"
qui replace county = "BEAVER" if city=="ALIQUIPPA" & state=="PA" & county=="."
qui replace county = "BEDFORD" if city=="EVERETT" & state=="PA" & county=="."
qui replace county = "BEDFORD" if county=="B" & city=="EVERETT" & state=="PA"
qui replace county = "BERKS" if city=="READING" & state=="PA" & county=="."
qui replace county = "BLAIR" if (city=="ALTOONA"|city=="TYRONE") & state=="PA" & county=="."
qui replace county = "BLAIR" if county=="B" & city=="ALTOONA" & state=="PA"
qui replace county = "BRADFORD" if (county=="BRADFORDCOUNTY"|county=="BRANDFOR.OUNTY") & state=="PA"
qui replace county = "BRADFORD" if city=="SAYRE" & state=="PA" & county=="."
qui replace county = "BUCKS" if (city=="DOYLESTOWN"|city=="SELLERSVILLE") & state=="PA" & county=="."
qui replace county = "CAMBRIA" if (city=="HASTINGS"|city=="JOHNSTOWN") & state=="PA" & county=="."
qui replace county = "CENTRE" if (city=="CENTRE HALL"|city=="PLEASANT GAP") & state=="PA" & county=="."
qui replace county = "CENTRE" if county=="CENTRE HALL" & state=="PA"
qui replace county = "CLARION" if county=="C" & state=="PA"
qui replace county = "CRAWFORD" if city=="TITUSVILLE" & state=="PA" & county=="."
qui replace county = "CUMBERLAND" if city=="MECHANICSBURG" & state=="PA" & county=="."
qui replace county = "DAUPHIN" if city=="HARRISBURG" & state=="PA" & county=="."
qui replace county = "DAUPHIN" if county=="DAUPHINAMPTON" & state=="PA"
qui replace county = "DELAWARE" if (city=="DARBY"|city=="HAVERTOWN") & state=="PA" & county=="."
qui replace county = "ELK" if (city=="RIDGEWAY"|city=="ST MARYS"|city=="ST.MARYS") & state=="PA" & county=="."
qui replace county = "ELK" if city=="RIDGWAY" & state=="PA"
qui replace county = "ERIE" if (city=="ERIE"|city=="UNION CITY") & state=="PA" & county=="."
qui replace county = "ERIE" if county=="E" & state=="PA"
qui replace county = "FAYETTE" if (city=="CONNELLSVILLE"|city=="CONNELSVILLE") & state=="PA" & county=="."
qui replace county = "FRANKLIN" if (county=="FRANKLINRY"|county=="FRAMKLIN") & state=="PA"
qui replace county = "FRANKLIN" if city=="WAYNESBORO" & state=="PA" & county=="."
qui replace county = "HUNTINGDON" if city=="HUNTINGDON" & state=="PA" & county=="."
qui replace county = "LACKAWANNA" if (city=="CLARKS SUMMIT"|city=="SCRANTON") & state=="PA" & county=="."
qui replace county = "LANCASTER" if (city=="COLUMBIA"|city=="LANCASTER") & state=="PA" & county=="."
qui replace county = "LANCASTER" if county=="LANCASTYER" & state=="PA"
qui replace county = "LEHIGH" if city=="ALLENTOWN" & state=="PA" & county=="."
qui replace county = "LEHIGH" if county=="OREFIELD" & state=="PA"
qui replace county = "LUZERNE" if (city=="HAZLETON"|city=="KINGSTON"|city=="WILKES-BARRE") & state=="PA" & county=="."
qui replace county = "LYCOMING" if (city=="MUNCY"|city=="WILLIAMSPORT") & state=="PA" & county=="."
qui replace county = "LYCOMING" if county=="LYCOMING " & state=="PA"
qui replace county = "MC KEAN" if county=="MCKEAN" & state=="PA"
qui replace county = "MERCER" if city=="GREENVILLE" & state=="PA" & county=="."
qui replace county = "MERCER" if county=="M" & state=="PA"
qui replace county = "MIFFLIN" if city=="LEWISTOWN" & state=="PA" & county=="."
qui replace county = "MONTGOMERY" if (city=="FORT WASHINGTON"|city=="FORT WASKINGTON"|city=="LANSDALE"|city=="NORRISTOWN"|city=="POTTSTOWN") & state=="PA" & county=="."
qui replace county = "MONTGOMERY" if county=="MONTOGOMERY" & state=="PA"
qui replace county = "MONTOUR" if city=="DANVILLE" & state=="PA" & county=="."
qui replace county = "MONTOUR" if county=="MOUNTOUR" & state=="PA"
qui replace county = "NORTHAMPTON" if (county=="NORTHHAMPTON"|county=="NORTH HAMPTON") & state=="PA"
qui replace county = "NORTHUMBERLND" if city=="SHAMOKIN" & state=="PA" & county=="."
qui replace county = "NORTHUMBERLND" if county=="NORTHUMBERLAND" & state=="PA"
qui replace county = "PHILADELPHIA" if (county=="PHILADELPIHA"|county=="PHILA"|county=="PHIL.") & state=="PA"
qui replace county = "PHILADELPHIA" if city=="PHILADELPHIA" & state=="PA" & county=="."
qui replace county = "PIKE" if city=="MILFORD" & state=="PA" & county=="."
qui replace county = "POTTER" if county=="POTTERCOUNTY" & state=="PA"
qui replace county = "SCHUYLKILL" if (city=="COALDALE"|city=="POTTSVILLE") & state=="PA" & county=="."
qui replace county = "SCHUYLKILL" if county=="SCHUYKILL" & state=="PA"
qui replace county = "SOMERSET" if (city=="MEYERSDALE"|city=="WINDBER") & state=="PA" & county=="."
qui replace county = "SOMERSET" if county=="S" & state=="PA"
qui replace county = "WESTMORELAND" if (city=="GREENSBURG"|city=="TORRANCE") & state=="PA" & county=="."
qui replace county = "WESTMORELAND" if county=="HARRIS" & state=="PA"
qui replace county = "YORK" if city=="YORK" & state=="PA" & county=="."

* Rhode Island 
qui replace county = "KENT" if city=="WARWICK" & state=="RI" & county=="."
qui replace county = "NEWPORT" if city=="NEWPORT" & state=="RI" & county=="."
qui replace county = "PROVIDENCE" if (city=="EAST PROVIDENCE"|city=="NORTH PROVIDENCE"|city=="PAWTUCKET"|city=="PROVIDENCE") & state=="RI" & county=="."
qui replace county = "PROVIDENCE" if county=="EAST. PROVIDENCE" & state=="RI"
qui replace county = "WASHINGTON" if (city=="WAKEFIELD"|city=="WESTERLY") & state=="RI" & county=="."
qui replace county = "WASHINGTON" if county=="SOUTH" & state=="RI"

* South Carolina 
qui replace county = subinstr(county," SOUTH CAROLINA","",.) if state=="SC"
qui replace county = "BEAUFORT" if city=="BEAUFORT" & state=="SC" & county=="."
qui replace county = "CHARLESTON" if (city=="CHARLESTON"|city=="N. CHARLESTON") & state=="SC" & county=="."
qui replace county = "CHARLESTON" if (county=="CHARLESTNON"|county=="CHARLESTON/DORCHESTER"|county=="CHARLESTOWN"|county=="CHARLSETON") & state=="SC"
qui replace county = "CHEROKEE" if city=="GAFFNEY" & state=="SC" & county=="."
qui replace county = "CHESTERFIELD" if city=="CHERAW" & state=="SC" & county=="."
qui replace county = "DARLINGTON" if county=="DARLINGOTN" & state=="SC"
qui replace county = "FAIRFIELD" if (county=="FAIRFIRLD"|county=="WINSBORO") & state=="SC"
qui replace county = "FLORENCE" if (city=="FLORENCE"|city=="LAKE CITY") & state=="SC" & county=="."
qui replace county = "FLORENCE" if county=="LAKE CITY" & state=="SC"
qui replace county = "GREENVILLE" if (city=="GREENVILLE"|city=="SIMPSONVILLE"|city=="TRAVELERS REST") & state=="SC" & county=="."
qui replace county = "GREENVILLE" if county=="29601" & state=="SC"
qui replace county = "GREENWOOD" if city=="GREENWOOD" & state=="SC" & county=="."
qui replace county = "HORRY" if (city=="CONWAY"|city=="MYRTLE BEACH") & state=="SC" & county=="."
qui replace county = "LANCASTER" if county=="LLANCASTER" & state=="SC"
qui replace county = "LEXINGTON" if city=="WEST COLUMBIA" & state=="SC" & county=="."
qui replace county = "MARLBORO" if county=="MALBORO" & state=="SC"
qui replace county = "NEWBERRY" if county=="N" & state=="SC"
qui replace county = "ORANGEBURG" if city=="ORANGEBURG" & state=="SC" & county=="."
qui replace county = "RICHLAND" if (county=="RIDGELAND"|county=="RICHLAN") & state=="SC"
qui replace county = "RICHLAND" if city=="COLUMBIA" & state=="SC" & county=="."
qui replace county = "SPARTANBURG" if city=="GREER" & state=="SC" & county=="."
qui replace county = "SUMTER" if city=="SUMTER" & state=="SC" & county=="."
qui replace county = "UNION" if county=="U" & state=="SC"
qui replace county = "YORK" if city=="ROCK HILL" & state=="SC" & county=="."

* South Dakota 
qui replace county = "BEADLE" if city=="HURON" & state=="SD" & county=="."
qui replace county = "BENNETT" if city=="MARTIN" & state=="SD" & county=="."
qui replace county = "BON HOMME" if (city=="SCOTLAND"|city=="TYNDALL") & state=="SD" & county=="."
qui replace county = "BROOKINGS" if city=="BROOKINGS" & state=="SD" & county=="."
qui replace county = "BROWN" if city=="ABERDEEN" & state=="SD" & (county=="."|county=="ROLETTE"|county=="SIOUX"|county=="UNKNOWN")
qui replace county = "BRULE" if county=="BRUEL" & state=="SD"
qui replace county = "CHARLES MIX" if (city=="PLATTE"|city=="WAGNER") & state=="SD" & county=="."
qui replace county = "CODINGTON" if city=="WATERTOWN" & state=="SD" & county=="."
qui replace county = "CUSTER" if city=="CUSTER" & state=="SD" & county=="."
qui replace county = "DAVISON" if city=="MITCHELL" & state=="SD" & county=="."
qui replace county = "DAY" if city=="WEBSTER" & state=="SD" & county=="."
qui replace county = "DEUEL" if city=="CLEAR LAKE" & state=="SD" & county=="."
qui replace county = "DEWEY" if city=="EAGLE BUTTE" & state=="SD" & county=="."
qui replace county = "DOUGLAS" if city=="ARMOUR" & state=="SD" & county=="."
qui replace county = "EDMUNDS" if city=="BOWDLE" & state=="SD" & county=="."
qui replace county = "FALL RIVER" if city=="HOT SPRINGS" & state=="SD" & county=="."
qui replace county = "FAULK" if city=="FAULKTON" & state=="SD" & county=="."
qui replace county = "HAAKON" if city=="PHILIP" & state=="SD" & county=="."
qui replace county = "HAAKON" if county=="HAKKON" & state=="SD"
qui replace county = "HAND" if city=="MILLER" & state=="SD" & county=="."
qui replace county = "HUTCHINSON" if (city=="FREEMAN"|city=="PARKSTON") & state=="SD" & county=="."
qui replace county = "JERAULD" if city=="WESSINGTON SPRINGS" & state=="SD" & county=="."
qui replace county = "KINGSBURY" if city=="DESMET" & state=="SD" & county=="."
qui replace county = "LAKE" if city=="MADISON" & state=="SD" & county=="."
qui replace county = "LAWRENCE" if (city=="DEADWOOD"|city=="SPEARFISH") & state=="SD" & county=="."
qui replace county = "LAWRENCE" if (county=="LAWERENCE"|county=="L") & state=="SD"
qui replace county = "LINCOLN" if (city=="CANTON"|city=="SIOUX FALLS") & state=="SD" & county=="."
qui replace county = "MARSHALL" if city=="BRITTON" & state=="SD" & county=="."
qui replace county = "MC PHERSON" if city=="EUREKA" & state=="SD" & county=="."
qui replace county = "MC PHERSON" if county=="MCPHERSON" & state=="SD"
qui replace county = "MEADE" if city=="STURGIS" & state=="SD" & county=="."
qui replace county = "MINNEHAHA" if city=="DELL RAPIDS" & state=="SD" & county=="."
qui replace county = "MINNEHAHA" if county=="MINNEHAHAH" & state=="SD"
qui replace county = "MOODY" if city=="FLANDREAU" & state=="SD" & county=="."
qui replace county = "PENNINGTON" if (county=="PEENINGTON"|county=="P") & state=="SD"
qui replace county = "PENNINGTON" if city=="RAPID CITY" & state=="SD" & county=="."
qui replace county = "PERKINS" if city=="LEMMON" & state=="SD" & county=="."
qui replace county = "POTTER" if (city=="GETTYSBURG"|city=="HOVEN") & state=="SD" & county=="."
qui replace county = "ROBERTS" if city=="SISSETON" & state=="SD" & county=="."
qui replace county = "SHANNON" if city=="PINE RIDGE" & state=="SD" & county=="."
qui replace county = "SPINK" if city=="REDFIELD" & state=="SD" & county=="."
qui replace county = "TODD" if city=="ROSEBUD" & state=="SD" & county=="."
qui replace county = "TRIPP" if city=="WINNER" & state=="SD" & county=="."
qui replace county = "TURNER" if city=="VIBORG" & state=="SD" & county=="."
qui replace county = "UNION" if city=="DAKOTA DUNES" & state=="SD" & county=="."
qui replace county = "WALWORTH" if city=="MOBRIDGE" & state=="SD" & county=="."
qui replace county = "YANKTON" if city=="YANKTON" & state=="SD" & county=="."

* Tennessee 
qui replace county = "BEDFORD" if city=="SHELBYVILLE" & state=="TN" & county=="."
qui replace county = "BLOUNT" if city=="LOUISVILLE" & state=="TN" & county=="."
qui replace county = "CAMPBELL" if county=="CAMBELL" & state=="TN"
qui replace county = "CANNON" if city=="WOODBURY" & state=="TN" & county=="."
qui replace county = "CARROLL" if county=="CARROL" & state=="TN"
qui replace county = "CARTER" if city=="ELIZABETHTON" & state=="TN" & county=="."
qui replace county = "CLAY" if city=="CELINA" & state=="TN" & county=="."
qui replace county = "COCKE" if city=="NEWPORT" & state=="TN" & county=="."
qui replace county = "COFFEE" if county=="COFFE" & state=="TN"
qui replace county = "DAVIDSON" if city=="NASHVILLE" & state=="TN" & county=="."
qui replace county = "DAVIDSON" if county=="NASHVILLE" & state=="TN"
qui replace county = "DE KALB" if county=="DEKALB" & state=="TN"
qui replace county = "FRANKLIN" if (county=="COFFEE-FRANKLIN"|county=="OFFEE-FRANKLIN") & state=="TN"
qui replace county = "GIBSON" if (city=="HUMBOLDT"|city=="HUMBOLT"|city=="TRENTON") & state=="TN" & county=="."
qui replace county = "GILES" if city=="PULASKI" & state=="TN" & county=="."
qui replace county = "GREENE" if (county=="GEENE"|county=="TAYLOR") & state=="TN"
qui replace county = "HAMBLEN" if city=="MORRISTOWN" & state=="TN" & county=="."
qui replace county = "HAMILTON" if city=="CHATTANOOGA" & state=="TN" & county=="."
qui replace county = "HAMILTON" if county=="HAMILITON"|county=="HAMTILTON" & state=="TN"
qui replace county = "HARDEMAN" if city=="BOLIVAR" & state=="TN" & county=="."
qui replace county = "HAWKINS" if (city=="KINGSPORT"|city=="ROGERSVILLE") & state=="TN" & county=="."
qui replace county = "HOUSTON" if city=="ERIN" & state=="TN" & county=="."
qui replace county = "HUMPHREYS" if city=="WAVERLY" & state=="TN" & county=="."
qui replace county = "HUMPHREYS" if county=="HUMPHEYS" & state=="TN"
qui replace county = "KNOX" if (county=="KNOW"|county=="KNOXVILLE") & state=="TN"
qui replace county = "KNOX" if city=="KNOXVILLE" & state=="TN" & county=="."
qui replace county = "LOUDON" if city=="LOUDON" & state=="TN" & county=="."
qui replace county = "MADISON" if county=="M" & city=="JACKSON" & state=="TN"
qui replace county = "MARSHALL" if county=="M" & city=="LEWISBURG" & state=="TN"
qui replace county = "MAURY" if city=="COLUMBIA" & state=="TN" & county=="."
qui replace county = "MC MINN" if city=="ATHENS" & state=="TN" & county=="."
qui replace county = "MC MINN" if county=="MCMINN" & state=="TN"
qui replace county = "MC NAIRY" if county=="MCNAIRY" & state=="TN"
qui replace county = "OBION" if city=="UNION CITY" & state=="TN" & county=="."
qui replace county = "OVERTON" if city=="LIVINGSTON" & state=="TN" & county=="."
qui replace county = "RUTHERFORD" if county=="RETHERFORD" & state=="TN"
qui replace county = "SHELBY" if (county=="MEMPHIS"|county=="SHEBLY"|county=="909") & state=="TN"
qui replace county = "SHELBY" if city=="MEMPHIS" & state=="TN" & county=="."
qui replace county = "SULLIVAN" if county=="SULIVAN" & state=="TN"
qui replace county = "SUMNER" if city=="HENDERSONVILLE" & state=="TN" & county=="."
qui replace county = "UNICOI" if county=="UCICOI" & state=="TN"
qui replace county = "WASHINGTON" if city=="JOHNSON CITY" & state=="TN" & county=="."
qui replace county = "WAYNE" if city=="WAYNESBORO" & state=="TN" & county=="."
qui replace county = "WEAKLEY" if city=="MARTIN" & state=="TN" & county=="."
qui replace county = "WEAKLEY" if county=="WEAKLY" & state=="TN"

* Texas 
qui replace county = "ANDERSON" if city=="PALESTINE" & state=="TX" & county=="."	
qui replace county = "ANDERSON" if county=="ANDERSEN" & state=="TX"	
qui replace county = "ANGELINA" if city=="LUFKIN" & state=="TX" & county=="."	
qui replace county = "ARANSAS" if county=="CORPUS CHRISTI" & state=="TX"	
qui replace county = "ATASCOSA" if city=="JOURDANTON" & state=="TX" & county=="."	
qui replace county = "BAILEY" if city=="MULESHOE" & state=="TX" & county=="."	
qui replace county = "BASTROP" if city=="SMITHVILLE" & state=="TX" & county=="."	
qui replace county = "BELL" if city=="TEMPLE" & state=="TX" & county=="."	
qui replace county = "BEXAR" if (city=="17720 CORPORATE WOODS DR"|city=="SAN ANTONIO") & state=="TX" & county=="."	
qui replace county = "BEXAR" if (county=="BEXAS"|county=="BAXER") & state=="TX"	
qui replace county = "BOWIE" if city=="TEXARKANA" & state=="TX" & county=="."	
qui replace county = "BRAZORIA" if city=="ALVIN" & state=="TX" & county=="."	
qui replace county = "BRAZOS" if county=="BARZOS" & state=="TX"	
qui replace county = "BROWN" if (county=="BROWNWOOD"|county=="HARRISOOD") & state=="TX"	
qui replace county = "BROWN" if city=="BROWNWOOD" & state=="TX" & county=="."	
qui replace county = "CALDWELL" if city=="LULING" & state=="TX" & county=="."	
qui replace county = "CAMERON" if (city=="BROWNSVILLE"|city=="HARLINGEN"|city=="SAN BENITO") & state=="TX" & county=="."	
qui replace county = "CASTRO" if city=="DIMMITT" & state=="TX" & county=="."	
qui replace county = "CHAMBERS" if (city=="ANAHUAC"|city=="BAYTOWN") & state=="TX" & county=="."	
qui replace county = "CHEROKEE" if city=="RUSK" & state=="TX" & county=="."	
qui replace county = "COLLIN" if (city=="MCKINNEY"|city=="PLANO") & state=="TX" & county=="."	
qui replace county = "COLLIN" if county=="COLLINS" & state=="TX"	
qui replace county = "COMAL" if county=="COMEL" & state=="TX"	
qui replace county = "COMANCHE" if city=="COMANCHE" & state=="TX" & county=="."	
qui replace county = "CONCHO" if city=="EDEN" & state=="TX" & county=="."	
qui replace county = "CORYELL" if city=="GATESVILLE" & state=="TX" & county=="."	
qui replace county = "CROSBY" if city=="CROSBYTON" & state=="TX" & (county=="."|county=="TEXAS")
qui replace county = "CULBERSON" if city=="VAN HORN" & state=="TX" & county=="."	
qui replace county = "DALLAM" if (county=="DALLAM - HARTLEY"|county=="DALLAM-HARTLEY") & state=="TX"	
qui replace county = "DALLAS" if (city=="DALLAS"|city=="DESOTO"|city=="GARLAND"|city=="IRVING"|city=="RICHARDSON") & state=="TX" & county=="."	
qui replace county = "DALLAS" if (county=="DALLAS/TARRANT"|county=="DALLAS/ TARRANT"|county=="DALLA"|county=="IRVING") & state=="TX"	
qui replace county = "DAVIS" if city=="BOUNTIFUL" & state=="TX" & county=="."	
qui replace county = "DE WITT" if city=="YOAKUM" & state=="TX" & county=="."	
qui replace county = "DE WITT" if county=="DEWITT" & state=="TX"	
qui replace county = "DIMMIT" if city=="CARRIZO SPRINGS" & state=="TX" & county=="."	
qui replace county = "EL PASO" if city=="EL PASO" & state=="TX" & county=="."	
qui replace county = "EL PASO" if county=="EL" & state=="TX"	
qui replace county = "FAYETTE" if city=="LA GRANGE" & state=="TX" & county=="."	
qui replace county = "FRIO" if city=="DILLEY" & state=="TX" & county=="."	
qui replace county = "GAINES" if city=="SEMINOLE" & state=="TX" & county=="."	
qui replace county = "GALVESTON" if city=="GALVESTON" & state=="TX" & county=="."	
qui replace county = "GONZALES" if city=="GONZALES" & state=="TX" & county=="."	
qui replace county = "GRAY" if city=="PAMPA" & state=="TX" & county=="."	
qui replace county = "GRAYSON" if (city=="DENISON"|city=="SHERMAN") & state=="TX" & county=="."	
qui replace county = "GREGG" if city=="LONGVIEW" & state=="TX" & county=="."	
qui replace county = "GUADALUPE" if city=="NEW BRAUNFELS" & state=="TX" & county=="."	
qui replace county = "HALE" if city=="HALE CENTER" & state=="TX" & county=="."	
qui replace county = "HAMILTON" if city=="HAMILTON" & state=="TX" & county=="."	
qui replace county = "HARRIS" if (city=="HOUSTON  TX 77030"|city=="HOUSTON"|city=="HUMBLE"|city=="KINGWOOD"|city=="NASSAU BAY"|city=="PASADENA"|city=="THE WOODLANDS"|city=="WEBSTER") & state=="TX" & county=="."	
qui replace county = "HARRIS" if (county=="EAST. BATON ROUGE"|county=="HARIS"|county=="HARRIST") & state=="TX"	
qui replace county = "HARRIS" if city!="MARSHALL" & state=="TX" & county=="H"	
qui replace county = "HARRISON" if city=="MARSHALL" & state=="TX" & county=="."	
qui replace county = "HARRISON" if city=="MARSHALL" & state=="TX" & county=="H"	
qui replace county = "HAYS" if county=="HAYES" & state=="TX"	
qui replace county = "HIDALGO" if (city=="MISSION"|city=="WESLACO") & state=="TX" & county=="."	
qui replace county = "HIDALGO" if county=="HILDAGO" & state=="TX"	
qui replace county = "HILL" if city=="WHITNEY" & state=="TX" & county=="."	
qui replace county = "HOWARD" if city=="BIG SPRING" & state=="TX" & county=="."	
qui replace county = "HUNT" if (city=="COMMERCE"|city=="GREENVILLE") & state=="TX" & county=="."	
qui replace county = "JASPER" if city=="JASPER" & state=="TX" & county=="."	
qui replace county = "JEFFERSON" if (city=="BEAUMONT"|city=="GROVES"|city=="PORT ARTHUR") & state=="TX" & county=="."	
qui replace county = "JIM WELLS" if city=="ALICE" & state=="TX" & county=="."	
qui replace county = "KARNES" if city=="KENEDY" & state=="TX" & county=="."	
qui replace county = "KAUFMAN" if (city=="MESQUITE"|city=="TERRELL") & state=="TX" & county=="."	
qui replace county = "KERR" if (county=="KERRVILLE"|county=="GREENE") & state=="TX"	
qui replace county = "KERR" if city=="KERRVILLE" & state=="TX" & county=="."	
qui replace county = "KNOX" if city=="KNOX CITY" & state=="TX" & county=="."	
qui replace county = "LAMAR" if city=="PARIS" & state=="TX" & county=="."	
qui replace county = "LIMESTONE" if city=="MEXIA" & state=="TX" & county=="."	
qui replace county = "LIMESTONE" if county=="LIMESTON" & state=="TX"	
qui replace county = "LUBBOCK" if city=="LUBBOCK" & state=="TX" & county=="."	
qui replace county = "MATAGORDA" if county=="MATAGOTDA" & state=="TX"	
qui replace county = "MAVERICK" if county=="MAVRICK" & state=="TX"	
qui replace county = "MC CULLOCH" if county=="MCCULLOCH" & state=="TX"	
qui replace county = "MC LENNAN" if city=="WEST" & state=="TX" & county=="."	
qui replace county = "MC LENNAN" if county=="MCLENNAN" & state=="TX"	
qui replace county = "MEDINA" if (county=="SAN ANTONIO"|county=="DADE CITY"|county=="BEAR") & state=="TX"	
qui replace county = "MEDINA" if city=="HONDO" & state=="TX" & county=="."	
qui replace county = "MIDLAND" if (county=="MIDLANDTON"|county=="ODESSA") & state=="TX"	
qui replace county = "MIDLAND" if city=="MIDLAND" & state=="TX" & county=="."	
qui replace county = "MILAM" if city=="CAMERON" & state=="TX" & county=="."	
qui replace county = "MONTGOMERY" if city=="CONROE" & state=="TX" & county=="."	
qui replace county = "NACOGDOCHES" if city=="NACOGDOCHES" & state=="TX" & county=="."	
qui replace county = "NAVARRO" if city=="CORSICANA" & state=="TX" & county=="."	
qui replace county = "NUECES" if (city=="ARANSAS PASS"|city=="CORPUS CHRISTI") & state=="TX" & county=="."	
qui replace county = "NUECES" if (county=="NEUCES"|county=="NUESES") & state=="TX"	
qui replace county = "ORANGE" if (county=="O	RANCE"|county=="ORANCE") & state=="TX"
qui replace county = "PARKER" if city=="WEATHERFORD" & state=="TX" & county=="."	
qui replace county = "PARMER" if city=="FRIONA" & state=="TX" & county=="."	
qui replace county = "RANDALL" if city=="AMARILLO" & state=="TX" & county=="."	
qui replace county = "REAGAN" if city=="BIG LAKE" & state=="TX" & county=="."	
qui replace county = "REFUGIO" if city=="REFUGIO" & state=="TX" & county=="."	
qui replace county = "ROCKWALL" if county=="ROWLETT" & state=="TX"	
qui replace county = "RUNNELS" if city=="WINTERS" & state=="TX" & county=="."	
qui replace county = "RUSK" if (city=="HENDERSON"|city=="KILGORE") & state=="TX" & county=="."	
qui replace county = "SAN AUGUSTINE" if county=="SAN" & state=="TX"	
qui replace county = "SAN PATRICIO" if county=="SAN PATRICO" & state=="TX"	
qui replace county = "SCURRY" if city=="SNYDER" & state=="TX" & county=="."	
qui replace county = "SMITH" if city=="TYLER" & state=="TX" & county=="."	
qui replace county = "SOMERVELL" if city=="GLEN ROSE" & state=="TX" & county=="."	
qui replace county = "TARRANT" if (city=="ARLINGTON"|city=="FORT WORTH"|city=="FT. WORTH"|city=="MANSFIELD"|city=="NORTH RICHLAND HILLS") & state=="TX" & county=="."	
qui replace county = "TARRANT" if (county=="TERRANT"|county=="TERRRANT"|county=="TARRENT"|county=="TARRRANT") & state=="TX"	
qui replace county = "TAYLOR" if city=="ABILENE" & state=="TX" & county=="."	
qui replace county = "TOM GREEN" if (county=="SAN ANGELO"|county=="TOM") & state=="TX"	
qui replace county = "TOM GREEN" if city=="SAN ANGELO" & state=="TX" & county=="."	
qui replace county = "TRAVIS" if (city=="AUSTIN"|city=="ROUND ROCK") & state=="TX" & county=="."	
qui replace county = "TRAVIS" if county=="TRAVIS CITY" & state=="TX"	
qui replace county = "TYLER" if city=="WOODVILLE" & state=="TX" & county=="."	
qui replace county = "UPTON" if (county=="RANKIN"|county=="UPTONN") & state=="TX"	
qui replace county = "UPTON" if city=="MCCAMEY" & state=="TX" & county=="."	
qui replace county = "VICTORIA" if city=="VICTORIA" & state=="TX" & county=="."	
qui replace county = "WARD" if city=="MONAHANS" & state=="TX" & county=="."	
qui replace county = "WASHINGTON" if city=="BRENHAM" & state=="TX" & county=="."	
qui replace county = "WEBB" if county=="07" & state=="TX"	
qui replace county = "WHARTON" if city=="WHARTON" & state=="TX" & county=="."	
qui replace county = "WHEELER" if city=="WHEELER" & state=="TX" & county=="."	
qui replace county = "WICHITA" if city=="WICHITA FALLS" & state=="TX" & county=="."	
qui replace county = "YOAKUM" if city=="DENVER CITY" & state=="TX" & county=="."	

* Utah 
qui replace county = "BEAVER" if city=="MILFORD" & state=="UT" & county=="."
qui replace county = "BOX ELDER" if (city=="BRIGHAM CITY"|city=="TREMONTON") & state=="UT" & county=="."
qui replace county = "BOX ELDER" if county=="BOXELDER" & state=="UT"
qui replace county = "DAVIS" if (city=="BOUNTIFUL"|city=="LAYTON"|city=="WOODS CROSS") & state=="UT" & county=="."
qui replace county = "GRAND" if county=="GRANDLLE" & state=="UT"
qui replace county = "JUAB" if city=="NEPHI" & state=="UT" & county=="."
qui replace county = "KANE" if city=="KANAB" & state=="UT" & county=="."
qui replace county = "MILLARD" if (city=="DELTA"|city=="FILLMORE") & state=="UT" & county=="."
qui replace county = "MILLARD" if county=="WEST. MILLARD" & state=="UT"
qui replace county = "SALT LAKE" if (city=="MIDVALE"|city=="SALT LAKE CITY"|city=="SANDY"|city=="WEST JORDAN") & state=="UT" & county=="."
qui replace county = "SALT LAKE" if (county=="SALTLAKE"|county=="WEST. JORDAN") & state=="UT"
qui replace county = "SANPETE" if (city=="GUNNISON"|city=="MT. PLEASANT") & state=="UT" & county=="."
qui replace county = "SANPETE" if (county=="GUNNISON"|county=="SANPETEN") & state=="UT"
qui replace county = "TOOELE" if city=="TOOELE" & state=="UT" & county=="."
qui replace county = "UINTAH" if county=="UNITAH" & state=="UT"
qui replace county = "UTAH" if (city=="PAYSON"|city=="PROVO"|city=="OREM") & state=="UT" & county=="."
qui replace county = "UTAH" if city=="OREM" & state=="UT" & county=="84057"
qui replace county = "UTAH" if city=="PROVO" & state=="UT" & (county==" UTAH"|county=="84603 UTAH")
qui replace county = "UTAH" if county=="OREM" & state=="UT"
qui replace county = "WASATCH" if city=="HEBER CITY" & state=="UT" & county=="84032 WASATCH"

* Virginia 
qui replace county = "ALBEMARLE" if (city=="CHARLOTTESVILLE"|city=="CHARLOTTSVILLE"|city=="FRANKLIN") & state=="VA" & county=="."
qui replace county = "ALBEMARLE" if county=="ALBERMARLE" & state=="VA"
qui replace county = "ALEXANDRIA CITY" if county=="ALEXANDRIA" & state=="VA"
qui replace county = "ALLEGHANY" if city=="LOW MOORE" & state=="VA" & county=="."
qui replace county = "ARLINGTON" if city=="ARLINGTON" & state=="VA" & county=="."
qui replace county = "AUGUSTA" if city=="FISHERSVILLE" & state=="VA" & county=="."
qui replace county = "AUGUSTA" if county=="AUGUSTA`" & state=="VA"
qui replace county = "BEDFORD" if city=="BEDFORD" & state=="VA" & county=="."
qui replace county = "CHESAPEAKE CITY" if county=="CHESAPEAKE" & state=="VA"
qui replace county = "CHESTERFIELD" if (county=="RICHMON / CHESTERFIELD"|county=="RICHMOND/CHESTERFIELD") & state=="VA"
qui replace county = "CULPEPER" if city=="CULPEPER" & state=="VA" & county=="."
qui replace county = "ESSEX" if city=="TAPPAHANNOCK" & state=="VA" & county=="."
qui replace county = "FAIRFAX CITY" if county=="FAIRFAXONA" & state=="VA"
qui replace county = "FAIRFAX" if (city=="ALEXANDRIA"|city=="FALLS CHURCH"|city=="RESTON") & state=="VA" & county=="."
qui replace county = "FREDERICKSBURG CITY" if city=="FREDERICKSBURG" & state=="VA" & county=="."
qui replace county = "FREDERICKSBURG CITY" if county=="FREDERICKSBURG" & state=="VA"
qui replace county = "GILES" if city=="PEARISBURG" & state=="VA" & county=="."
qui replace county = "GLOUCESTER" if city=="GLOUCESTER" & state=="VA" & county=="."
qui replace county = "HAMPTON CITY" if (city=="HAMPTION"|city=="HAMPTON") & state=="VA" & county=="."
qui replace county = "HAMPTON CITY" if (county=="HAMPTON"|county=="N/APTON") & state=="VA"
qui replace county = "HENRICO" if county==" HENRICO" & state=="VA"
qui replace county = "HOPEWELL CITY" if county=="HOPEWELL" & state=="VA"
qui replace county = "JAMES CITY" if city=="WILLIAMSBURG" & state=="VA" & county=="."
qui replace county = "LOUDOUN" if county=="LOUDON" & state=="VA"
qui replace county = "LYNCHBURG CITY" if county=="LYNCHBURG" & state=="VA"
qui replace county = "MONTGOMERY" if city=="RADFORD" & state=="VA" & county=="."
qui replace county = "NEW KENT" if city=="NEW KENT" & state=="VA" & county=="."
qui replace county = "NEWPORT NEWS CITY" if city=="NEWPORT NEWS" & state=="VA" & county=="."
qui replace county = "NEWPORT NEWS CITY" if county=="NEWPORT NEWS" & state=="VA"
qui replace county = "NORFOLK CITY" if (city=="CHESAPEAKE"|city=="NORFOLK"|city=="PORTSMOUTH") & state=="VA" & county=="."
qui replace county = "NORFOLK CITY" if (county=="CITY OF NORFOLK"|county=="NORFOLK") & state=="VA"
qui replace county = "NORTHAMPTON" if city=="NASSAWADOX" & state=="VA" & county=="."
qui replace county = "NORTON CITY" if city=="NORTON" & state=="VA" & county=="."
qui replace county = "NORTON CITY" if county=="NORTON" & state=="VA"
qui replace county = "PETERSBURG CITY" if (county=="CITY OF PETERSBURG"|county=="PETERSBURG") & state=="VA"
qui replace county = "PETERSBURG CITY" if city=="PETERSBURG" & state=="VA" & county=="."
qui replace county = "PORTSMOUTH CITY" if county=="PORTSMOUTH"  & state=="VA"
qui replace county = "RICHMOND CITY" if (city=="RICHMOND"|city=="SAGINAW") & state=="VA" & county=="."
qui replace county = "RICHMOND CITY" if (county=="CITY OF RICHMOND"|county=="RICHMOND") & state=="VA"
qui replace county = "ROANOKE" if (city=="ROANOKE"|city=="SALEM") & state=="VA" & county=="."
qui replace county = "SPOTSYLVANIA" if county=="SPOTTSYLVANIA" & state=="VA"
qui replace county = "SUFFOLK CITY" if (city=="SUFFOLK  VIRGINIA"|city=="SUFFOLK") & state=="VA" & county=="."
qui replace county = "SUFFOLK CITY" if county=="SUFFOLK" & state=="VA"
qui replace county = "TAZEWELL" if city=="RICHLANDS" & state=="VA" & county=="."
qui replace county = "VIRGINIA BEACH CITY" if (county=="VA BEACH"|county=="VIRGINIA BEACH") & state=="VA"
qui replace county = "VIRGINIA BEACH CITY" if city=="VIRGINIA BEACH" & state=="VA" & county=="."
qui replace county = "WYTHE" if city=="WYTHEVILLE" & state=="VA" & county=="."
qui replace county = "WYTHE" if county=="WYTHEVILLE" & state=="VA"

* Vermont 
qui replace county = "CALEDONIA" if county=="CALEDONA" & state=="VT"
qui replace county = "FRANKLIN" if city=="SAINT ALBANS" & state=="VT" & county=="."
qui replace county = "RUTLAND" if city=="RUTLAND" & state=="VT" & county=="."
qui replace county = "WASHINGTON" if city=="WATERBURY" & state=="VT" & county=="."
qui replace county = "WINDHAM" if city=="TOWNSHEND" & state=="VT" & county=="."
qui replace county = "WINDHAM" if county=="WINDHAN" & state=="VT"
qui replace county = "WINDSOR" if city=="WINDSOR" & state=="VT" & county=="."

* Washington 
qui replace county = "ASOTIN" if city=="CLARKSTON" & state=="WA" & county=="."
qui replace county = "BENTON" if city=="RICHLAND" & state=="WA" & county=="."
qui replace county = "CHELAN" if city=="LEAVENWORTH" & state=="WA" & county=="."
qui replace county = "FRANKLIN" if city=="PASCO" & state=="WA" & county=="."
qui replace county = "GRANT" if city=="GRAND COULEE" & state=="WA" & county=="."
qui replace county = "KING" if (city=="ENUMCLAW"|city=="KIRKLAND"|city=="SEATTLE") & state=="WA" & county=="."
qui replace county = "KITTITAS" if city=="ELLENSBURG" & state=="WA" & county=="."
qui replace county = "KLICKITAT" if city=="WHITE SALMON" & state=="WA" & county=="."
qui replace county = "PACIFIC" if county=="WILLAPA" & state=="WA"
qui replace county = "PIERCE" if city=="TACOMA" & state=="WA" & county=="."
qui replace county = "SPOKANE" if city=="SPOKANE" & state=="WA" & county=="."
qui replace county = "THURSTON" if city=="OLYMPIA" & state=="WA" & county=="."
qui replace county = "WHATCOM" if city=="BELLINGHAM" & state=="WA" & county=="."

* Wisconsin 
qui replace county = "BROWN" if city=="GREEN BAY" & state=="WI" & county=="."
qui replace county = "COLUMBIA" if county=="COLUMBI" & state=="WI"
qui replace county = "DANE" if county=="D" & state=="WI"
qui replace county = "DODGE" if city=="BEAVER DAM" & state=="WI" & county=="."
qui replace county = "EAU CLAIRE" if city=="EAU CLAIRE" & state=="WI" & county=="."
qui replace county = "FOND DU LAC" if (city=="FOND DU LAC"|city=="RIPON") & state=="WI" & county=="."
qui replace county = "GREEN" if city=="MONROE" & state=="WI" & county=="."
qui replace county = "JEFFERSON" if city=="WATERTOWN" & state=="WI" & county=="."
qui replace county = "LA CROSSE" if city=="LACROSSE" & state=="WI" & county=="."
qui replace county = "LA CROSSE" if county=="LACROSSE" & state=="WI"
qui replace county = "MARATHON" if city=="MARSHFIELD" & state=="WI" & county=="."
qui replace county = "MILWAUKEE" if (city=="BROWN DEER"|city=="MILWAUKEE"|city=="WEST ALLIS") & state=="WI" & county=="."
qui replace county = "PIERCE" if city=="RIVER FALLS" & state=="WI" & county=="."
qui replace county = "PIERCE" if county=="PIERCE STATES" & state=="WI"
qui replace county = "POLK" if city=="ST. CROIX FALLS" & state=="WI" & county=="."
qui replace county = "PORTAGE" if city=="STEVENS POINT" & state=="WI" & county=="."
qui replace county = "RACINE" if city=="WATERFORD" & state=="WI" & county=="."
qui replace county = "ST. CROIX" if (city=="BALDWIN"|city=="NEW RICHMOND") & state=="WI" & county=="."
qui replace county = "ST. CROIX" if (county=="ST. CROIS"|county=="S CROIX"|county=="ST.  CROIX") & state=="WI"
qui replace county = "TREMPEALEAU" if (county=="TREMPLEAU"|county=="TREMMPLEAU"|county=="TREMPEAULEAU") & state=="WI"
qui replace county = "TREMPEALEAU" if city=="ARCADIA" & state=="WI" & county=="."
qui replace county = "WASHBURN" if city=="SHELL LAKE" & state=="WI" & county=="."
qui replace county = "WAUKESHA" if (county=="WUAKESHA"|county=="WAUKSHA") & state=="WI"
qui replace county = "WAUKESHA" if city=="WAUKESHA" & state=="WI" & county=="."
qui replace county = "WAUPACA" if city=="WAUPACA" & state=="WI" & county=="."
qui replace county = "WINNEBAGO" if county=="WINNEBAGOD" & state=="WI"

* West Virginia 
qui replace county = "CABELL" if city=="HUNTINGTON" & state=="WV" & county=="."
qui replace county = "FAYETTE" if city=="OAK HILL" & state=="WV" & county=="."
qui replace county = "GREENBRIER" if (city=="RONCERVERTE"|city=="RONCEVERTE") & state=="WV" & county=="."
qui replace county = "KANAWHA" if (city=="CHARLESTON"|city=="SOUTH CHARLESTON") & state=="WV" & county=="."
qui replace county = "LEWIS" if city=="WESTON" & state=="WV" & county=="."
qui replace county = "MARION" if city=="FAIRMONT" & state=="WV" & county=="."
qui replace county = "MARSHALL" if city=="GLEN DALE" & state=="WV" & county=="."
qui replace county = "MC DOWELL" if county=="MCDOWELL" & state=="WV"
qui replace county = "MERCER" if city=="PRINCETON" & state=="WV" & county=="."
qui replace county = "MINERAL" if city=="KEYSER" & state=="WV" & county=="."
qui replace county = "MONONGALIA" if city=="MORGANTOWN" & state=="WV" & county=="."
qui replace county = "MONONGALIA" if county=="MONOGALIA" & state=="WV"
qui replace county = "RALEIGH" if city=="BECKLEY" & state=="WV" & county=="."
qui replace county = "SUMMERS" if city=="HINTON" & state=="WV" & county=="."
qui replace county = "WOOD" if city=="PARKERSBURG" & state=="WV" & county=="."
qui replace county = "WOOD" if county=="WOODTON" & state=="WV"

* Wyoming
qui replace county = "LARAMIE" if city=="CHEYENNE" & state=="WY" & county=="."
qui replace county = "NATRONA" if county=="NATRONAD" & state=="WY"
qui replace county = "PARK" if city=="POWELL" & state=="WY" & county=="."
qui replace county = "PARK" if (county=="CODY"|county=="POWELL") & state=="WY"
qui replace county = "UINTA" if city=="EVANSTON" & state=="WY" & (county=="82930"|county==".")


